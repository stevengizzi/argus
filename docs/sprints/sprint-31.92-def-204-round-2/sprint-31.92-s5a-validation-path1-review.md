<!-- workflow-version: 1.2.0 -->
# Tier 2 Review: Sprint 31.92, Session S5a — Path #1 In-Process Falsifiable Validation Against SimulatedBroker

## Instructions

You are conducting a Tier 2 code review. This is a READ-ONLY session.
Do NOT modify any source code files.

Follow the review skill in `.claude/skills/review.md`.

Your review report MUST include a structured JSON verdict at the end,
fenced with ```json:structured-verdict. See the review skill for the
full schema and requirements.

**Write the review report to a file** (DEC-330):

```
docs/sprints/sprint-31.92-def-204-round-2/sprint-31.92-s5a-review.md
```

Create the file, write the full report (including the structured JSON
verdict) to it, and commit it. This is the ONE exception to "do not
modify any files" — the review report file is the sole permitted write.

## Pre-Flight

1. **Read `.claude/rules/universal.md` in full and treat its contents as binding for this review.** The full set of universal RULE entries (currently RULE-001 through RULE-053) applies regardless of whether any specific rule is referenced inline in this prompt — particularly RULE-013 (read-only mode), RULE-038 (grep-verify factual claims), RULE-040 (small-sample sweep / spike-artifact directional vs. authoritative — load-bearing because S5a is in-process logic validation, NOT production safety evidence), RULE-050 (CI green precondition), and RULE-051 (mechanism-signature-vs-symptom validation discipline — load-bearing for the AMD-2 mechanism-conditional alignment check).

## Review Context

Read the following file for the Sprint Spec, Specification by Contradiction,
Sprint-Level Regression Checklist, and Sprint-Level Escalation Criteria:

`docs/sprints/sprint-31.92-def-204-round-2/review-context.md`

## Tier 1 Close-Out Report

Read the close-out report from:

```
docs/sprints/sprint-31.92-def-204-round-2/sprint-31.92-s5a-closeout.md
```

If the close-out file does not exist, flag this as CONCERNS — the close-out report is required for review per DEC-330.

## Review Scope

- **Diff to review:** `git diff HEAD~1`
- **Test command** (DEC-328 — non-final session, scoped):
  ```
  python -m pytest tests/integration/ tests/execution/ -n auto -q
  ```
- **Files that should NOT have been modified (S5a is TEST-ONLY — production code shows ZERO edits):**
  - `argus/execution/order_manager.py`
  - `argus/execution/simulated_broker.py` (semantic preservation per SbC §"Do NOT modify" #2 — fixtures live in test code only)
  - `argus/main.py`
  - `argus/execution/ibkr_broker.py`
  - `argus/core/config.py`
  - `argus/core/health.py`, `argus/api/routes/alerts.py`, `argus/api/websocket/alerts_ws.py`
  - `argus/models/trading.py`
  - `argus/execution/alpaca_broker.py`
  - `argus/data/alpaca_data_service.py`
  - `frontend/`, `argus/ui/` (regression invariant 12 / B-class halt B8)
  - DB schemas
  - DEC-385 / DEC-386 / DEC-388 entries in `docs/decision-log.md`
  - `workflow/` submodule (RULE-018)

  **Permitted modifications:** new test file `tests/integration/test_def204_round2_validation.py` + new orchestrator script `scripts/validate_def204_round2_path1.py` + autogenerated artifact `scripts/spike-results/sprint-31.92-validation-path1.json`.

## Session-Specific Review Focus

1. **Production code untouched.** S5a is a test/validation-only session. `git diff HEAD~1 -- argus/` MUST return empty (or only touch test directories). Verify via diff inspection. Any production-code edit is a B-class halt B6 candidate.

2. **AC5.1 in-process scope qualifier preserved.** This is the load-bearing framing per RULE-040. The script docstring AND the close-out AND the JSON artifact must ALL explicitly state: "validates IN-PROCESS LOGIC against SimulatedBroker; does NOT validate IBKR-API-interaction logic." Treating the artifact as production safety evidence is the failure mode this gate prevents. Cessation criterion #5 (5 paper sessions clean post-seal) remains the production-validation gate — the close-out must NOT claim S5a closes that gate.

3. **Mechanism alignment — RULE-051 mechanism-signature vs symptom-aggregate (regression invariant 17).** The `mechanism_under_test` field in the validation JSON MUST match the `selected_mechanism` field in `scripts/spike-results/spike-def204-round2-path1-results.json`. If they diverge, the validation is testing a different mechanism than what S2a/S2b shipped — that is the silent-drift failure mode RULE-051 catches. Verify by reading both JSONs:
   ```bash
   python -c "import json; print(json.load(open('scripts/spike-results/spike-def204-round2-path1-results.json'))['selected_mechanism'])"
   python -c "import json; print(json.load(open('scripts/spike-results/sprint-31.92-validation-path1.json'))['mechanism_under_test'])"
   ```
   They must be byte-equal.

4. **Synthetic clock only (B-class halt B10).** No real wall-clock `asyncio.sleep` in the fixture. Scan the test file for `asyncio.sleep(<positive_real>)` calls; any hit with positive duration >100ms is a B10 candidate (per-test runtime ≤ 5s threshold). The composite Path #1 fixture must use synthetic-clock loops, not unmocked propagation.

5. **JSON artifact schema completeness.** The validation JSON has all required fields:
   - `validation_run_date` (ISO 8601 UTC)
   - `mechanism_under_test` (one of `"h2_amend_stop"` / `"h4_hybrid"` / `"h1_cancel_and_await"`)
   - `fixture` (`"bitu_13_41_concurrent_trigger"`)
   - `path1_safe` (boolean — must be `true` at S5a close-out)
   - `phantom_shorts_observed` (integer — must be 0)
   - `total_sold_le_total_bought` (boolean — must be `true`)
   - `n_trials` (integer)
   - `argus_baseline_sha` (string — `git rev-parse HEAD` short)
   - `spike_path1_artifact_sha` (string — sha of the spike artifact)
   Test 4 asserts this; reviewer confirms test 4's assertion list is exhaustive (uses `assert "field_name" in json_data` for each, not a substring search).

6. **Composite validation pattern (per session-breakdown).** The 4 tests in `tests/integration/test_def204_round2_validation.py` form the canonical Path #1 validation harness. Verify:
   - Test 1: `path1_safe=true` assertion.
   - Test 2: `phantom_shorts_observed == 0` assertion.
   - Test 3: `total_sold_le_total_bought == true` assertion.
   - Test 4: artifact-on-disk schema completeness + mechanism alignment with spike artifact.
   Each test independently triggers the validation script (or its module-level entrypoint) — one-call drives all four assertions if the script is deterministic.

7. **Mechanism-agnostic fixture.** The `_BituConcurrentTriggerFixture` (or equivalent) must assert AC1.1 invariant (no phantom shorts, total_sold ≤ total_bought) regardless of HOW the S2a-selected mechanism prevents over-flatten. The test should NOT branch on `mechanism_under_test == "h1_cancel_and_await"` vs `"h2_amend_stop"` vs `"h4_hybrid"` — that branching is forbidden because it couples the validation to a specific mechanism choice.

8. **Fixture lives in test code (SbC §"Do NOT modify" #2).** `_BituConcurrentTriggerFixture` (or equivalent) MUST live in `tests/integration/test_def204_round2_validation.py`, NOT in `argus/execution/simulated_broker.py`. Production `simulated_broker.py` is byte-for-byte unchanged.

9. **Per-test runtime ≤ 5s (B10).** Run `pytest --durations=10 tests/integration/test_def204_round2_validation.py` and confirm each new test is below 5s. The Path #1 concurrent-trigger fixture should be sub-second via synthetic-clock loops.

10. **Validation script LOC budget.** `scripts/validate_def204_round2_path1.py` should be ≤150 LOC. Excess is a compaction-risk signal — flag if the script grew beyond budget.

## Sprint-Level Regression Checklist (for @reviewer)

The full Sprint-Level Regression Checklist is in `docs/sprints/sprint-31.92-def-204-round-2/regression-checklist.md` (34 invariants total).

✓-mandatory at S5a per the Per-Session Verification Matrix:

- **Invariant 1 (DEC-117 atomic bracket):** PASS — production code untouched.
- **Invariant 2 (DEC-364 cancel_all_orders no-args ABC):** PASS.
- **Invariant 5 (DEC-385 6-layer side-aware reconciliation):** PASS.
- **Invariant 6 (DEC-386 4-layer OCA architecture):** PASS.
- **Invariant 9 (`# OCA-EXEMPT:` mechanism):** PASS.
- **Invariant 10 (test count baseline ≥ 5,269; target +4):** PASS.
- **Invariant 11 (pre-existing flake count):** PASS — DEF-150/167/171/190/192.
- **Invariant 12 (frontend immutability):** PASS.
- **Invariant 15 (`_OCA_TYPE_BRACKET` constant deleted; S4b precondition):** PASS — preserved.
- **Invariant 17 (AMD-2 mechanism-conditional):** VALIDATES — S5a's `mechanism_under_test` matches the S2a-shipped mechanism.
- **Invariant 18 (spike artifacts fresh + committed):** ESTABLISHES — S5a's artifact joins the freshness checklist.

## Visual Review (N/A — backend-only)

S5a is backend-only. No frontend changes per regression invariant 12.

## Additional Context

- S5a validates the Path #1 mechanism (trail-stop / bracket-stop concurrent-trigger over-flatten prevention) in-process against SimulatedBroker. **It is NOT a production safety claim** — IBKR-API timing, network packet loss during cancel/amend propagation, and `modifyOrder` response timing are all out of scope. Cessation criterion #5 (5 paper sessions clean post-seal) is the production-validation gate.
- The validation harness becomes a daily CI freshness check at sprint-close (operator-manual workflow YAML edit per `doc-update-checklist.md` C9 — NOT in S5a's diff).
- A3 (composite validation produces phantom_shorts > 0) halts the sprint if S5a's `phantom_shorts_observed != 0` OR `total_sold_le_total_bought == false`. Same failure class that ended Sprint 31.91's `~98%` claim. Tier 3 reviews which session's coverage was insufficient — DO NOT seal the sprint on A3 fire.
