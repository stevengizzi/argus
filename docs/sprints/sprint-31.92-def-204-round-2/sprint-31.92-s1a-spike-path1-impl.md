# Sprint 31.92, Session 1a: Path #1 Mechanism Spike — H2 modify_order Rejection-Rate + 4 Adversarial Axes + N=100 Cancel-Then-Immediate-SELL Stress

> **Phase A diagnostic spike. Non-safe-during-trading. Run pre-market or after-hours.** This is a read-only spike against paper IBKR (account U24619949, clientId=1) and the ARGUS broker code. NO production code is modified. The session emits a single Python script + autogenerated JSON artifact whose `selected_mechanism` field gates Phase D implementation prompts S2a + S2b. If the spike returns `status: INCONCLUSIVE`, A-class halt **A1** fires: surface to operator before any S2a/S2b prompt is generated.

## Pre-Flight Checks

Before making any changes:

1. **Read `.claude/rules/universal.md` in full and treat its contents as binding for this session.** The full set of universal RULE entries (currently RULE-001 through RULE-053) applies regardless of whether any specific rule is referenced inline in this prompt. Of particular relevance to S1a: **RULE-038** (grep-verify discipline at session start), **RULE-040** (small-sample sweep conclusions are directional only — but here the spike is the falsifying mechanism for FAI #3 + FAI #5, so the directional-vs-decision framing inverts: the spike's HARD GATE on N=100 trials and worst-axis Wilson UB are decision inputs by construction), **RULE-050** (CI green required at session close-out), **RULE-053** (architecturally-sealed documents — DEC-386's 4-layer OCA architecture is sealed and S1a must NOT modify any sealed surface).

2. Read these files to load context. Pre-flight reads are listed in priority order; load all five before touching the script.
   - `scripts/spike_ibkr_oca_late_add.py` — S1a's reference pattern from DEC-386 Phase A. Mirror the file shape: argparse-driven async-main + per-trial timing capture + JSON-emit-at-end. Pre-flight grep-verify:
     ```bash
     grep -n "^def \|^async def \|^def parse_args\|def _classify_outcome\|async def main_async" scripts/spike_ibkr_oca_late_add.py
     ```
     Expected: ~10 hits including `parse_args`, `main_async`, `_classify_outcome`, `_run_one_trial`. (Directional only — verify via grep.)
   - `argus/execution/ibkr_broker.py` — anchor by class `IBKRBroker` and methods `cancel_all_orders`, `modify_order` (the H2 amend-stop-price entry point) and `place_bracket_order`. Pre-flight grep-verify:
     ```bash
     grep -n "class IBKRBroker\|async def cancel_all_orders\|async def modify_order\|async def place_bracket_order" argus/execution/ibkr_broker.py
     ```
     Expected: 4 hits. (Directional only — verify via grep.)
   - `argus/execution/order_manager.py` — the hot path in scope is `_trail_flatten` (Path #1 BITU 13:41 trace canonical site). Pre-flight grep-verify:
     ```bash
     grep -n "async def _trail_flatten\|async def _resubmit_stop_with_retry\|async def _escalation_update_stop" argus/execution/order_manager.py
     ```
     Expected: 3 hits. (Directional only — verify via grep.)
   - `docs/debriefs/2026-04-28-paper-session-debrief.md` — Phase 7 §Path #1 BITU trace. The empirical falsification of DEC-386 (60 NEW phantom shorts) is the proximal trigger for Sprint 31.92's existence; this section gives the operational anchor for what the spike's measurements must inform. No grep-verify required — read for context.
   - `docs/sprints/sprint-31.92-def-204-round-2/sprint-spec.md` §"Hypothesis Prescription" — especially H2 confirms-if conditions and the H-R2-2-tightened halt-or-proceed gate language reproduced in this prompt's "Halt-or-Proceed Decision Rule" section below. No grep-verify required — read for the decision contract the JSON output encodes.

3. Run the test baseline (DEC-328 — Session 1 of sprint, full suite required):

   ```bash
   python -m pytest --ignore=tests/test_main.py -n auto -q
   ```

   Expected: **5,269 tests, all passing**. The spike does not modify production code, so the baseline must be unchanged at close-out. If pytest count differs from 5,269 at session start, halt and surface to operator before running the spike.

   Note: In autonomous mode, the expected test count is dynamically adjusted by the runner based on the previous session's actual results. The count above is the planning-time estimate.

4. Verify you are on the correct branch: **`main`**.

5. **Run the structural-anchor grep-verify commands** from the "Files to Modify" section below. For each entry, run the verbatim grep-verify command and confirm the anchor still resolves to the expected location. If drift is detected, disclose under RULE-038 in the close-out and proceed against the actual structural anchors. If the anchor is not found at all, HALT and request operator disposition rather than guess.

6. **Verify paper IBKR Gateway accessibility** before script execution: account U24619949, clientId=1 (clientId=2 is reserved for parallel S1b execution). Market must be CLOSED (run pre-market or after-hours per "non-safe-during-trading" constraint). If S1b is running in parallel against the same Gateway, the operator confirms clientId budget split before S1a starts.

7. **Verify Sprint 31.91 + 31.915 SEALED at HEAD:**
   ```bash
   git log --oneline -20 | grep -E "Sprint 31\.915|Sprint 31\.91 SEALED"
   ```
   Expected: at least one match for Sprint 31.91 seal AND one for Sprint 31.915 seal. If absent, halt — Sprint 31.92 cannot start without prerequisites.

## Objective

Run a Phase A diagnostic spike against paper IBKR and emit a single JSON artifact (`scripts/spike-results/spike-def204-round2-path1-results.json`) whose `selected_mechanism` field deterministically resolves to one of `h2_amend` (PRIMARY DEFAULT), `h4_hybrid` (FALLBACK), or `h1_cancel_and_await` (LAST-RESORT) per the Hypothesis Prescription decision rule, OR returns `status: INCONCLUSIVE` to halt sprint progression. Measurements span four adversarial axes (concurrent / reconnect-window / stale-ID / NEW joint reconnect+concurrent) plus an N=100 cancel-then-immediate-SELL stress test that is a HARD GATE on H1 eligibility regardless of H2/H4 measurements.

## Requirements

1. **Create `scripts/spike_def204_round2_path1.py`** (~280 LOC, single file, mirroring `scripts/spike_ibkr_oca_late_add.py` shape):
   - Argparse-driven entry point. Required arguments: `--account`, `--client-id` (default 1), `--num-trials-per-axis` (default 50), `--n-stress-trials` (default 100), `--output-json` (default `scripts/spike-results/spike-def204-round2-path1-results.json`). Optional: `--symbols` (operator-provided list of liquid paper-tradeable symbols), `--dry-run`.
   - Async-main pattern via `asyncio.run(main_async(args))`. The script connects to paper IBKR Gateway, runs all measurement modes serially (NOT parallel — single Gateway client; serial avoids reqId collisions), emits one JSON artifact, disconnects, returns 0 (PROCEED) or 1 (INCONCLUSIVE).
   - **Measurement Mode A — H2 baseline `modify_order` round-trip latency + rejection rate + deterministic propagation.** ≥50 trials. For each trial: place a long entry + bracket (via `IBKRBroker.place_bracket_order`); wait for confirmed fill; capture `stop_order_id`; call `IBKRBroker.modify_order(stop_order_id, new_aux_price=current_price - $0.50)`; measure round-trip ms; record success/rejection; verify the broker-side bracket stop's `auxPrice` actually reflects the new value (deterministic propagation check via fresh `reqOpenOrders()` query); cancel-all and flatten between trials.
   - **Measurement Mode B — H2 adversarial axes (FAI #3 / Decision 1 / Round 3 M-R3-1):** four sub-axes, each ≥30 trials.
     - Axis (i) **concurrent amends across N≥3 positions** — open 3+ positions simultaneously, fire `modify_order` on all stops within ≤10ms; record per-position rejection rate.
     - Axis (ii) **amends during reconnect window** — operator-orchestrated Gateway disconnect (e.g., script pause + manual restart) within the trial; fire `modify_order` immediately on reconnect; record rejection rate during the reconnect window.
     - Axis (iii) **amends with stale order IDs** — synthesize a stale `stop_order_id` (e.g., capture an order ID from a closed bracket) and fire `modify_order` against it; record exact rejection signature.
     - Axis (iv) **NEW per Round 3 M-R3-1: joint reconnect+concurrent** — concurrent amends across N≥3 positions DURING a reconnect window. The composite stresses both axes (i) and (ii) simultaneously and is the worst-case operational scenario for H2.
   - **Measurement Mode C — H1 cancel-and-await latency.** ≥50 trials. For each: open a long entry + bracket; call `IBKRBroker.cancel_all_orders(symbol, await_propagation=True)`; measure round-trip ms; record success/timeout. Captures the H1 fallback baseline.
   - **Measurement Mode D — N=100 cancel-then-immediate-SELL stress (FAI #5 / Decision 2 — HARD GATE).** Exactly **100 trials**. For each: open a long entry + bracket; await fill confirmation; call `cancel_all_orders(symbol, await_propagation=True)`; **within ≤10ms of cancel return** (use `time.monotonic()`-based timing), call `place_order(SELL, qty=position_shares)`. Record whether ANY trial in 100 exhibits a bracket-child OCA conflict, locate suppression, position state inconsistency, or any other broker-side conflict signature. **`h1_propagation_zero_conflict_in_100` is True iff zero of 100 trials exhibit a conflict.**

2. **Compute worst-axis Wilson upper bound across the four adversarial axes.** Use Wilson 95% upper-confidence-bound formula on the binary success/rejection per-axis distributions from Mode B; the worst-axis Wilson UB is the maximum across the four axes. Report `adversarial_axes_results: dict` with one entry per axis (keys: `concurrent_amends`, `reconnect_window_amends`, `stale_id_amends`, `joint_reconnect_concurrent_amends`); each entry contains `n_trials`, `n_rejections`, `rejection_rate_pct`, `wilson_upper_bound_pct`. Report `worst_axis_wilson_ub: float` as the maximum across the four `wilson_upper_bound_pct` values.

3. **Apply the H2/H4/H1 decision rule** (verbatim Hypothesis Prescription gate language reproduced under "Halt-or-Proceed Decision Rule" below) to compute `selected_mechanism` and `status`:
   - If `worst_axis_wilson_ub < 5%` AND `h1_propagation_zero_conflict_in_100 == true`: `selected_mechanism = "h2_amend"`, `status = "PROCEED"`.
   - If `5% ≤ worst_axis_wilson_ub < 20%` AND `h1_propagation_zero_conflict_in_100 == true`: `selected_mechanism = "h4_hybrid"`, `status = "PROCEED"`.
   - If `worst_axis_wilson_ub ≥ 20%` AND `h1_propagation_zero_conflict_in_100 == true`: `selected_mechanism = "h1_cancel_and_await"`, `status = "PROCEED"` (BUT operator confirmation in writing required before S2a/S2b prompt generation per existing tightened gate language).
   - If `h1_propagation_zero_conflict_in_100 == false` (any 1 conflict in 100 trials): H1 is NOT eligible regardless of `worst_axis_wilson_ub`. If worst-axis Wilson UB < 20%, fall back to H2 or H4 per the bracket. If ≥ 20%, `status = "INCONCLUSIVE"` and surface to operator with explicit "H1 unsafe" determination.
   - Findings outside H2/H4/H1 (e.g., `modify_order` non-deterministic propagation; bracket stop's `auxPrice` doesn't update on Mode A trials): `status = "INCONCLUSIVE"`.

4. **Emit JSON artifact** at `scripts/spike-results/spike-def204-round2-path1-results.json` with these REQUIRED keys (all keys must be present even if a value is null when the spike halts mid-run):
   - `status`: `"PROCEED"` or `"INCONCLUSIVE"`.
   - `selected_mechanism`: `"h2_amend"`, `"h4_hybrid"`, or `"h1_cancel_and_await"`. If `status == "INCONCLUSIVE"`, this MAY be null with explanatory `inconclusive_reason: str`.
   - `h2_modify_order_p50_ms`: float, p50 round-trip latency from Measurement Mode A.
   - `h2_modify_order_p95_ms`: float, p95 round-trip latency from Measurement Mode A.
   - `h2_rejection_rate_pct`: float, baseline rejection rate (Mode A — pre-adversarial).
   - `h2_deterministic_propagation`: bool, True iff every Mode A trial's `auxPrice` update propagates broker-side within ≤500ms.
   - `adversarial_axes_results`: dict with 4 keys (per Requirement 2 above): `concurrent_amends`, `reconnect_window_amends`, `stale_id_amends`, `joint_reconnect_concurrent_amends`.
   - `worst_axis_wilson_ub`: float, maximum Wilson UB across the four axes.
   - `h1_cancel_all_orders_p50_ms`: float, from Measurement Mode C.
   - `h1_cancel_all_orders_p95_ms`: float, from Measurement Mode C.
   - `h1_propagation_n_trials`: int, exactly **100** (HARD GATE per Decision 2; do not reduce).
   - `h1_propagation_zero_conflict_in_100`: bool (HARD GATE per Decision 2; True iff zero conflicts in 100 trials).
   - `trial_count`: int, total trial count across all measurement modes (sanity check).
   - `spike_run_date`: ISO 8601 UTC timestamp at spike completion.

   The JSON file is autogenerated by the script — do NOT pre-create it. The script's exit code (0 = PROCEED, 1 = INCONCLUSIVE) gates downstream prompt generation; the JSON's `status` field is the authoritative record.

5. **Document the operator-confirmation requirement for H1 selection.** Under the H1 path (`selected_mechanism == "h1_cancel_and_await"` AND `status == "PROCEED"`), the close-out MUST surface this explicitly: H1 selection requires operator written confirmation before S2a/S2b implementation prompts are generated. This is the existing tightened gate language preserved through Round 3 M-R3-1; the operator's confirmation is the "explicit and logged" step. If H2 or H4 is selected, no additional operator gate.

## Files to Modify

This is a spike session; **no production files are modified**. The session creates two artifacts:

1. **`scripts/spike_def204_round2_path1.py`** — NEW file (~280 LOC).
   - Anchor: top-level module file (does not exist yet).
   - Edit shape: file creation.
   - Pre-flight grep-verify:
     ```bash
     test -f scripts/spike_def204_round2_path1.py && echo "EXISTS — halt" || echo "OK — file absent, ready to create"
     ```
     Expected: "OK — file absent, ready to create". If "EXISTS — halt" appears, halt and surface to operator (a prior session may have started this work).

2. **`scripts/spike-results/spike-def204-round2-path1-results.json`** — NEW file, autogenerated by the script at end-of-run.
   - Anchor: emitted by the script's final `json.dump()` call.
   - Edit shape: file creation (autogenerated).
   - Pre-flight grep-verify:
     ```bash
     test -f scripts/spike-results/spike-def204-round2-path1-results.json && echo "EXISTS — must overwrite" || echo "OK — file absent, ready to autogenerate"
     ```
     Either result is acceptable; the script overwrites unconditionally.

Line numbers MAY appear in this prompt as directional cross-references; they are NEVER the sole anchor. Where line numbers appear, they are flagged "directional only — verify via grep" with the grep command immediately following.

## Constraints

- Do NOT modify any file under `argus/`. This is a read-only spike with respect to production code. Specifically forbidden:
  - `argus/execution/order_manager.py` (hot path; modified only by S2a/S2b under Phase D after this spike's mechanism selection lands).
  - `argus/execution/ibkr_broker.py` (broker surface; the spike calls existing methods only).
  - `argus/execution/simulated_broker.py` (out of scope per SbC §"Out of Scope" #18).
  - `argus/main.py` (Sprint 31.94 surfaces; SbC §"Out of Scope" #5).
  - `argus/core/config.py` (config additions land in S3a/S4b, not S1a).
  - `argus/models/trading.py`.
- Do NOT modify any DEC-385/386/388 surface listed in `spec-by-contradiction.md` §"Out of Scope" items 2–4. The spike calls `place_bracket_order` + `cancel_all_orders` + `modify_order` as a CONSUMER; it does not modify them. A-class halt **A4** fires if the diff shows any change to these surfaces.
- Do NOT modify `frontend/`. Vitest count must remain at 913 unchanged. B-class halt **B8** fires if violated.
- Do NOT modify `tests/`. Spike session adds zero new pytest tests. The JSON artifact's required-keys list is the close-out validation surface, not a pytest assertion.
- Do NOT reduce `h1_propagation_n_trials` below 100. Decision 2's HARD GATE is N=100 trials; reducing the trial count silently weakens the gate (Wilson UB on 0/30 is roughly [0%, 11.6%]; on 0/100 it tightens substantially).
- Do NOT silently absorb spike failures. If any measurement mode fails to complete (e.g., paper IBKR Gateway disconnects mid-trial AND does not recover), halt the spike, write partial JSON with `status: "INCONCLUSIVE"` + `inconclusive_reason: "<failure description>"`, and surface to operator. Do NOT estimate missing measurements or extrapolate from partial data.
- Do NOT run S1a during market hours. The spike is non-safe-during-trading. If unavoidable circumstances place it during market hours, halt and re-schedule.
- Do NOT cross-reference other session prompts. This prompt is standalone. Even sessions with clear coupling (e.g., S2a's mechanism selection consumes this spike's JSON) handle the coupling via prose instructions referencing the state of `main`, not via cross-prompt references.

## Operator Choice

S1a does not require operator pre-check at session start. The mechanism choice is determined by the spike's empirical measurements per the H-R2-2-tightened decision rule. **The operator's confirmation gate fires only AFTER the spike completes and ONLY in the H1 path** (`selected_mechanism == "h1_cancel_and_await"`); see Requirement 5 above for the operator-confirmation contract.

## Halt-or-Proceed Decision Rule (verbatim from sprint-spec.md § Hypothesis Prescription, H-R2-2-tightened)

> Compute Wilson UB from observed rejection rate AS WELL AS the cancel-then-immediate-SELL stress sub-spike outcomes. **Decision rule (per Decision 2 — N=100 hard gate; per Round 3 M-R3-1 — worst-axis across 4 adversarial axes):** pick H2 if **worst-axis Wilson UB** (per Decision 1 + Round 3 M-R3-1 — adversarial axes (i)/(ii)/(iii) + NEW axis (iv) joint reconnect+concurrent: concurrent amends across N≥3 positions DURING reconnect window) **< 5%** AND `h1_propagation_zero_conflict_in_100 == true`. Pick H4 if 5% ≤ worst-axis Wilson UB < 20% AND `h1_propagation_zero_conflict_in_100 == true`. Pick H1 ONLY IF `h1_propagation_zero_conflict_in_100 == true` AND worst-axis Wilson UB ≥ 20% AND operator confirms H1 selection in writing per existing tightened gate language. **HARD GATE: if even 1 trial in 100 exhibits a conflict (`h1_propagation_zero_conflict_in_100 == false`), H1 is NOT eligible regardless of Wilson UB; surface to operator with explicit "H1 unsafe" determination and require alternative architectural fix (likely Sprint 31.94 D3 or earlier).** If findings are inconclusive or fall outside the enumerated hypotheses, HALT and write the diagnostic findings file with status INCONCLUSIVE; surface to operator before proceeding. Do NOT ship a Phase B fix that doesn't address the Phase A finding. **The hierarchy is H2 > H4 > H1 — safety drives the order, not operator preference.**

The escape-hatch language is load-bearing. The implementing session is explicitly authorized to deviate from the spec-prescribed measurement modes when an operational constraint warrants it (e.g., paper IBKR Gateway behaves anomalously and a different probing pattern surfaces the same primitive-semantics signal more reliably), provided the deviation is called out in the close-out's "Judgment Calls" section with cross-reference to the spike artifact.

## Test Targets

After implementation:

- **Existing tests:** all 5,269 must still pass. The spike adds zero modifications to production code; the test baseline must be unchanged at close-out.
- **New tests to write:** **0** (zero). This is a spike session — measurements emit a JSON artifact; the artifact's required-keys list is the close-out validation surface, not a pytest assertion.
- **Test command (close-out — DEC-328 mandates full suite at every close-out):**
  ```bash
  python -m pytest --ignore=tests/test_main.py -n auto -q
  ```
  Expected: 5,269 passing, identical to baseline.

The spike script itself is NOT a pytest test. It is a script invoked manually (during the operator-orchestrated paper-hours window) and emits the JSON artifact as its primary deliverable.

## Config Validation

S1a does not add config fields. No config-validation test required.

## Marker Validation

S1a does not add pytest markers. No marker-validation step required.

## Definition of Done

- [ ] `scripts/spike_def204_round2_path1.py` exists (~280 LOC) and matches the structural pattern of `scripts/spike_ibkr_oca_late_add.py`.
- [ ] Script runs against paper IBKR Gateway (account U24619949, clientId=1) without crashing and produces an exit code (0 PROCEED / 1 INCONCLUSIVE).
- [ ] `scripts/spike-results/spike-def204-round2-path1-results.json` exists, has all 14 REQUIRED keys (status, selected_mechanism, h2_modify_order_p50_ms, h2_modify_order_p95_ms, h2_rejection_rate_pct, h2_deterministic_propagation, adversarial_axes_results, worst_axis_wilson_ub, h1_cancel_all_orders_p50_ms, h1_cancel_all_orders_p95_ms, h1_propagation_n_trials, h1_propagation_zero_conflict_in_100, trial_count, spike_run_date), and `status: "PROCEED"`.
- [ ] If `selected_mechanism == "h1_cancel_and_await"`: operator has confirmed in writing per the existing tightened gate language (close-out captures the confirmation reference).
- [ ] `adversarial_axes_results` contains all 4 axes (concurrent_amends, reconnect_window_amends, stale_id_amends, joint_reconnect_concurrent_amends) with non-null `wilson_upper_bound_pct` values.
- [ ] `h1_propagation_n_trials == 100` (HARD GATE compliance).
- [ ] Full pytest suite at 5,269 (or current baseline) — unchanged from session start.
- [ ] CI green on the session's final commit.
- [ ] Close-out report written to file at `docs/sprints/sprint-31.92-def-204-round-2/session-1a-closeout.md`.
- [ ] Tier 2 review completed via @reviewer subagent and verdict CLEAR (or CONCERNS_RESOLVED if findings addressed in-session).

## Regression Checklist (Session-Specific)

After implementation, verify each of these:

| Check | How to Verify |
|-------|---------------|
| `git diff HEAD~1 -- argus/` returns empty | Spike modifies zero production code; A-class halts A4 / B6 fire if diff is non-empty. |
| `git diff HEAD~1 -- frontend/` returns empty | Frontend immutability invariant 12; B-class halt B8. |
| `git diff HEAD~1 -- tests/` returns empty | Spike adds zero new tests; baseline must be untouched. |
| Pytest count = baseline (5,269 expected) | `python -m pytest --ignore=tests/test_main.py -n auto -q --collect-only \| tail -3`; invariant 10 + B3. |
| Pre-existing flake count unchanged | Run full suite; check DEF-150, DEF-167, DEF-171, DEF-190, DEF-192 categories; invariant 11 + B1. |
| JSON artifact has all 14 required keys | `python -c "import json; d = json.load(open('scripts/spike-results/spike-def204-round2-path1-results.json')); required = {'status','selected_mechanism','h2_modify_order_p50_ms','h2_modify_order_p95_ms','h2_rejection_rate_pct','h2_deterministic_propagation','adversarial_axes_results','worst_axis_wilson_ub','h1_cancel_all_orders_p50_ms','h1_cancel_all_orders_p95_ms','h1_propagation_n_trials','h1_propagation_zero_conflict_in_100','trial_count','spike_run_date'}; missing = required - set(d); assert not missing, missing"`; invariant 18. |
| `h1_propagation_n_trials == 100` | Inspect JSON; HARD GATE per Decision 2. |
| `adversarial_axes_results` has 4 keys | Inspect JSON for `concurrent_amends`, `reconnect_window_amends`, `stale_id_amends`, `joint_reconnect_concurrent_amends`. |
| `status: "PROCEED"` (not INCONCLUSIVE) at session close | If INCONCLUSIVE, A-class halt A1 fires before session may close. |
| Spike artifact dated within last 24 hours | Operational regression invariant 18; `stat -f %m scripts/spike-results/spike-def204-round2-path1-results.json` (macOS) or `stat -c %Y` (Linux). |

## Close-Out

After all work is complete, follow the close-out skill in `.claude/skills/close-out.md`.

The close-out report MUST include a structured JSON appendix at the end, fenced with ` ```json:structured-closeout `. See the close-out skill for the full schema and requirements. **For this spike session, the structured JSON appendix's `artifacts` array MUST reference both `scripts/spike_def204_round2_path1.py` (the script) and `scripts/spike-results/spike-def204-round2-path1-results.json` (the JSON artifact); the appendix's `judgment_calls` field MUST surface (a) any deviation from the prescribed measurement modes per RULE-038 disclosure, (b) the operator-confirmation reference if H1 was selected, and (c) the spike's `selected_mechanism` value verbatim.**

**Write the close-out report to a file** (DEC-330):

```
docs/sprints/sprint-31.92-def-204-round-2/session-1a-closeout.md
```

Do NOT just print the report in the terminal. Create the file, write the full report (including the structured JSON appendix) to it, and commit it.

## Tier 2 Review (Mandatory — @reviewer Subagent)

After the close-out is written to file and committed, invoke the @reviewer subagent to perform the Tier 2 review within this same session.

Provide the @reviewer with:

1. The review context file: `docs/sprints/sprint-31.92-def-204-round-2/review-context.md`
2. The close-out report path: `docs/sprints/sprint-31.92-def-204-round-2/session-1a-closeout.md`
3. The diff range: `git diff HEAD~1`
4. The test command (non-final session, scoped per DEC-328): `python -m pytest tests/execution/ -n auto -q`
   (Note: spike script is not a pytest unit test; reviewer additionally verifies the JSON artifact's required-keys schema via the regression-checklist commands above.)
5. Files that should NOT have been modified:
   - All `argus/**/*` (entire production tree)
   - `frontend/`
   - `tests/`
   - `workflow/` submodule
   - Any sealed/frozen artifact in `docs/sprints/sprint-31.91-reconciliation-drift/` or `docs/sprints/sprint-31.915-evaluation-db-retention/`

The @reviewer will produce its review report (including a structured JSON verdict fenced with ` ```json:structured-verdict `) and write it to:

```
docs/sprints/sprint-31.92-def-204-round-2/session-1a-review.md
```

## Post-Review Fix Documentation

If the @reviewer reports CONCERNS and you fix the findings within this same session, you MUST update the artifact trail per the standard implementation-prompt template's "Post-Review Fix Documentation" section: append a "Post-Review Fixes" section to the close-out file; append a "Post-Review Resolution" section to the review file; update the structured verdict JSON to `CONCERNS_RESOLVED` and add a `post_review_fixes` array.

If the reviewer reports CLEAR or ESCALATE, skip this section entirely. ESCALATE findings must NOT be fixed without human review.

## Session-Specific Review Focus (for @reviewer)

1. **Verify the script does not modify production code.** `git diff HEAD~1 -- argus/` must return empty. If any `argus/**/*` file appears in the diff, A-class halt A4 fires regardless of close-out claims.
2. **Verify the JSON artifact's required-keys schema.** Run the inspection command in the regression checklist; assert all 14 keys present and `status: "PROCEED"`.
3. **Verify `h1_propagation_n_trials == 100`.** This is the HARD GATE per Decision 2; reducing N silently weakens the gate. C-class halt C8 (JSON schema deviation) escalates to operator if N < 100.
4. **Verify `worst_axis_wilson_ub` is the maximum across 4 axes,** not the average or median. The decision rule is worst-case-driven, not central-tendency-driven; misreading the JSON aggregator silently misroutes mechanism selection.
5. **Verify operator-confirmation reference if H1 was selected.** If `selected_mechanism == "h1_cancel_and_await"`, the close-out's "Judgment Calls" section MUST cite the operator's written confirmation per the existing tightened gate language. Absence escalates to operator before any S2a/S2b prompt generation.
6. **Verify the spike's adversarial axes were ACTUALLY adversarial.** The Round 3 reviewer's protocol step 3 (FAI completeness) requires the spike to PROBE for the breaking condition, not measure normal conditions. Inspect the script's axis (i) — concurrent amends across N≥3 positions — to confirm the timing actually overlaps (≤10ms separation between fire times) rather than serializing accidentally. If the script serializes the amends, the adversarial axis is degenerate and `worst_axis_wilson_ub` is meaningless; halt and request re-spike.
7. **Verify the spike artifact dates within last 24 hours of close-out.** Operational regression invariant 18: `scripts/spike-results/spike-def204-round2-path1-results.json` mtime should be within the session window. Stale artifacts indicate a previous run was committed without re-execution.

## Sprint-Level Regression Checklist (for @reviewer)

The full Sprint-Level Regression Checklist (34 invariants) is in `docs/sprints/sprint-31.92-def-204-round-2/regression-checklist.md`. Per the Per-Session Verification Matrix, S1a is ✓-MANDATORY for **invariants 10, 11, 12, 18**. The relevant invariant text is reproduced below verbatim for the reviewer's checklist (cross-reference the source file for full context).

### Invariant 10. Test Count Baseline Holds

**Test:** Full suite pytest count ≥ 5,269 at every close-out. Vitest count = 913 unchanged.

**Verified at:** Every session's close-out. **B-class halt B3** fires if pytest drops below baseline.

**Sessions responsible:** ALL.

**Per-session test deltas (S1a row):** 0 pytest, 0 Vitest. Final target: 5,357–5,403 pytest at S5c close-out.

### Invariant 11. Pre-Existing Flake Count Does Not Increase

**Test:** Run full suite at every session close-out; count failures attributable to DEF-150, DEF-167, DEF-171, DEF-190, DEF-192. Flake count ≤ baseline.

**Verified at:** Every session's close-out. **B-class halt B1** fires if any flake worsens or new flake appears.

**Sessions responsible:** ALL.

### Invariant 12. Frontend Immutability

**Test:** `git diff <session-base>..HEAD -- 'frontend/'` returns empty for every session. Vitest count = 913 at every close-out.

**Verified at:** Every session's close-out. **B-class halt B8** fires if violated.

**Sessions responsible:** ALL.

### Invariant 18. Spike Artifacts Committed and Fresh at First Post-Merge Paper Session

**Test:** Operational regression. Before the first post-Sprint-31.92-seal paper session boots:
- `scripts/spike-results/spike-def204-round2-path1-results.json` exists, has `status: PROCEED`, dated within 30 days, includes `adversarial_axes_results`, `worst_axis_wilson_ub`, `h1_propagation_n_trials=100`, `h1_propagation_zero_conflict_in_100: bool` (per Decisions 1 + 2).
- (Other validation/path2 artifacts land later; not S1a's responsibility.)

**Verified at:** Sprint-close (D14 doc-sync) AND before each subsequent paper session for 30 days post-seal. **A-class halt A13** fires if any artifact >30 days old at any future trigger event.

**Sessions responsible:** S1a (path1 spike).

## Sprint-Level Escalation Criteria (for @reviewer)

The full Sprint-Level Escalation Criteria are in `docs/sprints/sprint-31.92-def-204-round-2/escalation-criteria.md`. The relevant criteria for S1a are reproduced below verbatim. **CRITICAL safety sprint — bias toward halt over push-through.**

### A. Mandatory Halts (Tier 3 architectural review automatically fires)

| # | Trigger | Required Response |
|---|---------|-------------------|
| **A1** | **Spike S1a returns `status: INCONCLUSIVE`** in `scripts/spike-results/spike-def204-round2-path1-results.json` (i.e., findings fall outside H2/H4/H1 from Sprint Spec §"Hypothesis Prescription"). | Halt before S2a impl. Operator arranges Tier 3 review of the alternative mechanism (e.g., a hypothesis the spec author did not enumerate). The S2a/S2b implementation prompts MUST NOT be generated until Tier 3 selects a mechanism. Per protocol §"Hypothesis Prescription": "Do NOT ship a Phase B fix that doesn't address the Phase A finding." |
| **A4** | **Any session's diff modifies a DEC-385/386/388 surface listed in the SbC "Do NOT modify" list** beyond explicit byte-for-byte preservation. | Halt. RULE-004 + SbC violation. Revert the unintended change. If the change is necessary, escalate — Tier 3 reviews whether the SbC needs amendment OR whether the mechanism itself was wrong. |
| **A6** | **Any Tier 2 review on any session produces verdict = CONCERNS or ESCALATE.** | Halt. Operator + Tier 2 reviewer disposition the finding before next session begins. CONCERNS → typically iterate within the same session. ESCALATE → operator decides whether to revert the session's commit or fix forward. |
| **A9** | **Path #1 spike S1a measures worst-axis Wilson UB ≥20%** OR observes ≥2 trials where amend semantics non-deterministic. | Halt before S2a (under H2 path). Tier 3 evaluates whether the H2 amend mechanism is unreliable enough that H1 cancel-and-await (last-resort) is the actual default. **HARD GATE per Decision 2:** if `h1_propagation_zero_conflict_in_100 == false` (any 1 conflict in 100 trials), H1 is NOT eligible regardless of `modifyOrder` Wilson UB; surface to operator with explicit "H1 unsafe" determination and require alternative architectural fix (likely Sprint 31.94 D3 or earlier). If S1a's H1 measurement is also ≥500ms p95 OR observes non-convergence: Tier 3 evaluates a deeper architectural question — does ARGUS need to suspend Sprint 31.92 in favor of Sprint 31.94 reconnect-recovery first (which may be the structural cause of the broker latency drift)? |
| **A13** | **Spike artifact older than 30 days at the time of the FIRST post-merge paper session**, or `ib_async`/IBKR Gateway upgraded between spike-run and paper-session. | Halt the post-merge paper session. Operator re-runs both spikes. If status changes (e.g., `modifyOrder` Wilson UB shifted, OR `h1_propagation_zero_conflict_in_100` flipped): the closure is invalidated; halt sprint, re-plan affected sessions. Informational at S1a (no firing surface yet); referenced for completeness. |

### B. Mandatory Halts (Tier 3 not required, but operator + Tier 2 reviewer must disposition)

| # | Trigger | Required Response |
|---|---------|-------------------|
| **B1** | **Pre-existing flake count increases.** Specifically: any of DEF-150, DEF-167, DEF-171, DEF-190, DEF-192 fail more frequently than baseline OR a NEW intermittent failure appears that doesn't have a DEF entry yet (RULE-041). | Halt. File DEF entry with symptom + repro hint + xdist worker context. Operator decides whether to fix in this sprint or defer. |
| **B3** | **Pytest baseline ends below 5,269** at the close-out of any session. | Halt. The new tests are additive; existing baseline must hold. Investigate which test broke and either fix or revert. |
| **B4** | **CI fails on the session's final commit and the failure is NOT a documented pre-existing flake.** | Halt per RULE-050. Do not proceed to next session until CI green is achieved. RULE-052: if CI is red for a known-cosmetic reason, log that assumption explicitly at each subsequent commit. |
| **B5** | **Structural anchor (function name / class name / regex pattern) referenced in impl prompt does not match repo state during pre-flight grep-verify.** | Halt mid-pre-flight. Re-anchor the session's surgical edits against actual current structural anchors. RULE-038 grep-verify discipline. Mark the finding as RESOLVED-VERIFIED if change is already in place, or flag the discrepancy in the close-out. |
| **B6** | **A do-not-modify-list file appears in the session's `git diff`.** | Halt. RULE-004 violation. Revert the unintended change before close-out. If the change is necessary, escalate to operator. |
| **B7** | **Test runtime degrades >2× from baseline** OR a single test's runtime exceeds 60 seconds. | Halt and investigate. Per RULE-037, verify no orphaned background processes are inflating runtime. **N/A for S1a spike** (no new tests added) but full-suite close-out runtime is checked. |
| **B8** | **Any session's diff modifies `frontend/`** OR Vitest count changes from baseline 913. | Halt. SbC §"Do NOT modify" violation. Sprint 31.92 has zero frontend scope. Revert. |

### C. Soft Halts (Continue with extra caution + close-out flag)

| # | Trigger | Required Response |
|---|---------|-------------------|
| **C1** | **Implementer notices a bug or improvement opportunity OUTSIDE the current session's scope.** | Document in close-out under "Deferred Items" (RULE-007). Do NOT fix in this session. |
| **C2** | **First fix attempt fails and a second approach emerges.** | Per RULE-008, allow ONE additional approach attempt without operator approval; if that also fails, halt and request guidance. |
| **C3** | **Two failed fix attempts on the same bug.** | Switch to diagnostic-first mode (RULE-030). Build a minimal repro outside the application code. Close-out marks Context State YELLOW. |
| **C4** | **Context State trending YELLOW** (long conversation, many file reads, many edits). | Per RULE-027, proactively checkpoint: commit, run scoped tests, produce partial close-out, suggest continuing in a fresh session. |
| **C8** | **S1a spike output JSON schema does not exactly match the schema specified in the spec** (e.g., field names differ slightly because the operator-developer revised them during the spike). | Continue. Document the actual schema in the close-out. The downstream impl prompts at S2a/S2b consume the JSON; minor schema deviations are acceptable as long as the gating fields (`status`, `selected_mechanism`, `worst_axis_wilson_ub`, `h1_propagation_zero_conflict_in_100`) are present and meaningful. |

### Sprint Abort Conditions (relevant to S1a)

The sprint as a whole is aborted (not just an individual session) if ANY of:

1. **Three or more A-class halts within the same week.** Indicates the spec or the underlying mechanism understanding is wrong; the sprint needs replanning.
2. **Mechanism turns out to be different from S1a/S1b spike findings** (DEBUNKED status). Pivot to a Sprint 31.92.5 or 31.93-prefix sprint with new diagnostic.

### Verification Grep Precision

When kickoffs include verification grep commands, prefer the more precise patterns:

- **Section counting:** use `^## [1-9]\.` (anchor + literal dot) rather than `^## [0-9]` to avoid double-counting `## 10.`, `## 11.`, etc.
- **Human-authored content with TitleCase:** use `grep -i` for content like Markdown bold-list section names. Lowercase patterns will return false negatives against TitleCase source.
- **Token-presence checks across rejection-framed content:** when checking that a rejected pattern is absent, scan only validation logic, not docstrings/rationale blocks. A rejection rationale that names the token is not a reintroduction.

## Section Ordering

This template's section order matches the implementation execution order: Pre-Flight → Objective → Requirements → Files to Modify → Constraints → Operator Choice → Halt-or-Proceed Decision Rule → Test Targets → Definition of Done → Regression Checklist → Close-Out → Tier 2 Review → Post-Review Fix Documentation → Session-Specific Review Focus → Sprint-Level Regression Checklist → Sprint-Level Escalation Criteria.

Visual order matches execution order so Claude Code instances following the template top-to-bottom proceed in the correct sequence. Do NOT reorder sections — the close-out report's self-assessment gates whether commit happens, and Tier 2 review runs after commit.
