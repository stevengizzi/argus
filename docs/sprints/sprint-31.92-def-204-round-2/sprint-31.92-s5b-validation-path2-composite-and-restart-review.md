<!-- workflow-version: 1.2.0 -->
# Tier 2 Review: Sprint 31.92, Session S5b — Path #2 + Composite + Restart-Scenario Falsifiable Validation

## Instructions

You are conducting a Tier 2 code review. This is a READ-ONLY session.
Do NOT modify any source code files.

Follow the review skill in `.claude/skills/review.md`.

Your review report MUST include a structured JSON verdict at the end,
fenced with ```json:structured-verdict. See the review skill for the
full schema and requirements.

**Write the review report to a file** (DEC-330):

```
docs/sprints/sprint-31.92-def-204-round-2/sprint-31.92-s5b-review.md
```

Create the file, write the full report (including the structured JSON
verdict) to it, and commit it. This is the ONE exception to "do not
modify any files" — the review report file is the sole permitted write.

## Pre-Flight

1. **Read `.claude/rules/universal.md` in full and treat its contents as binding for this review.** The full set of universal RULE entries (currently RULE-001 through RULE-053) applies regardless of whether any specific rule is referenced inline in this prompt — particularly RULE-013 (read-only mode), RULE-038 (grep-verify factual claims), RULE-040 (small-sample sweep / spike-artifact directional vs. authoritative), RULE-050 (CI green precondition), and RULE-053 (architectural-seal verification — DEC-385 6-layer reconciliation + DEC-386 4-layer OCA + DEC-388 alert observability are sealed defenses; the validation tests assert composition holds, NOT that any layer is modified).

## Review Context

Read the following file for the Sprint Spec, Specification by Contradiction,
Sprint-Level Regression Checklist, and Sprint-Level Escalation Criteria:

`docs/sprints/sprint-31.92-def-204-round-2/review-context.md`

## Tier 1 Close-Out Report

Read the close-out report from:

```
docs/sprints/sprint-31.92-def-204-round-2/sprint-31.92-s5b-closeout.md
```

If the close-out file does not exist, flag this as CONCERNS — the close-out report is required for review per DEC-330.

## Review Scope

- **Diff to review:** `git diff HEAD~1`
- **Test command** (DEC-328 — non-final session, scoped):
  ```
  python -m pytest tests/integration/ tests/execution/order_manager/ -n auto -q
  ```
- **Files that should NOT have been modified (S5b is TEST-ONLY — production code shows ZERO edits):**
  - `argus/execution/order_manager.py`
  - `argus/execution/simulated_broker.py` (the `SimulatedBrokerWithRefreshTimeout` fixture lands at S5c per Tier 3 item E / DEF-SIM-BROKER-TIMEOUT-FIXTURE — NOT at S5b)
  - `argus/execution/ibkr_broker.py` (post-S3a state; `_is_locate_rejection` helper preserved)
  - `argus/main.py`
  - `argus/core/config.py`
  - `argus/core/health.py`, `argus/api/v1/alerts.py`, `argus/ws/v1/alerts.py`
  - `.github/workflows/` (sprint-close operator-manual task per `doc-update-checklist.md` C9 — NOT an S5b modification)
  - `frontend/`, `argus/ui/` (regression invariant 12 / B-class halt B8)
  - DB schemas
  - DEC-385 / DEC-386 / DEC-388 entries in `docs/decision-log.md`
  - `workflow/` submodule (RULE-018)
  - **The 4 S5a tests in `tests/integration/test_def204_round2_validation.py` — append-only.** S5b appends 9 new tests; the 4 prior tests must remain byte-for-byte unchanged.

  **Permitted modifications:** new orchestrator script `scripts/validate_def204_round2_path2.py` + autogenerated artifacts `scripts/spike-results/sprint-31.92-validation-path2.json` + `scripts/spike-results/sprint-31.92-validation-composite.json` + 9 new tests appended to `tests/integration/test_def204_round2_validation.py`.

## Session-Specific Review Focus

1. **Production code untouched.** S5b is a test/validation-only session. `git diff HEAD~1 -- argus/` MUST return empty (or only touch test directories). Any production-code edit is a B-class halt B6 candidate.

2. **`.github/workflows/` untouched.** Daily CI workflow YAML is a sprint-close operator task per `doc-update-checklist.md` C9 — NOT in S5b's diff. Verify via `git diff HEAD~1 -- '.github/workflows/'` returning empty.

3. **Append-only to S5a test file.** S5a's 4 tests (`test_validate_path1_*`) MUST remain byte-for-byte unchanged. Verify via `git diff HEAD~1 -- tests/integration/test_def204_round2_validation.py` showing only end-of-file additions. Use `grep -c "^def test_" tests/integration/test_def204_round2_validation.py` — expect exactly 13 hits post-S5b (4 from S5a + 9 from S5b).

4. **Composite JSON write-before-assert ordering (AC5.3 freshness contract).** Test 4 (`test_composite_validation_zero_phantom_shorts_under_load`) fixture must call `json.dump(...)` to `scripts/spike-results/sprint-31.92-validation-composite.json` BEFORE the `assert phantom_shorts_observed == 0` line. Reviewer reads test 4 source line-by-line (NOT via string search); this ordering is the AC5.3 freshness contract — if the assertion fails, the artifact still captures the failure state for operator triage.

5. **Canonical C-2 RESTART TEST exhaustive (AC5.4 / regression invariant 19).** Test 8 (`test_restart_during_active_position_refuses_argus_sells`) must invoke ALL 4 standalone-SELL paths:
   - `_trail_flatten`
   - `_flatten_position`
   - `_check_flatten_pending_timeouts`
   - `_escalation_update_stop`
   Reviewer counts the assertion calls; if any path is missing, this is the canonical C-2 coverage hole that A-class halt A15 fires on in production. The `is_reconstructed=True` refusal posture (AC3.7) is the structural defense; failure means the conservative posture is leaking. The test must also assert that the operator-manual flatten path (`scripts/ibkr_close_all_positions.py`) is NOT blocked by the refusal posture (the refusal applies to ARGUS-emitted SELLs ONLY).

6. **Reconstruct-from-broker initialization fields (AC3.6 + AC3.7 / regression invariant 19).** Test 9 (`test_reconstruct_from_broker_initializes_is_reconstructed_true_and_zero_counters`) must assert initialization of FOUR fields:
   - `cumulative_pending_sell_shares = 0`
   - `cumulative_sold_shares = 0`
   - `is_reconstructed = True`
   - `shares_total = abs(broker_position.shares)`
   Reviewer confirms test 9's assertion list is exhaustive.

7. **Per-`ManagedPosition` (NOT per-symbol) ceiling (AC3.4).** Test 6 explicitly creates two distinct `ManagedPosition`s on the same symbol (with distinct ULIDs) and asserts each has its own ceiling. Reviewer verifies the test's setup constructs distinct ULIDs — if both positions share the same ULID, the test's per-position scope assertion is tautological.

8. **Cross-position safety preserved (AC2.4 — deferred from S3b per H-2).** Test 7 (`test_composite_validation_multi_position_same_symbol_cross_safety`) creates two `ManagedPosition`s on the same symbol, triggers locate-rejection on position A, asserts position B's `_locate_suppressed_until` entry is NOT affected. Reviewer verifies the test's assertion is on a position-keyed dict lookup, not a symbol-keyed lookup (the latter would be a Round-1 H-2 regression).

9. **Deferred ceiling-emit-site coverage from S4a-i (AC3.2 — remaining 4 of 5 sites).** Test 5 (`test_composite_validation_ceiling_blocks_under_adversarial_load`) must exercise forced over-flatten attempts at all 5 standalone-SELL emit sites (`_trail_flatten`, `_escalation_update_stop`, `_resubmit_stop_with_retry`, `_flatten_position`, `_check_flatten_pending_timeouts`); assert `ceiling_violations_correctly_blocked >= 1`. The canonical site at `_trail_flatten` was covered at S4a-i; S5b extends to the remaining 4.

10. **No real wall-clock sleeps (B10).** Reviewer scans new test code for `asyncio.sleep(<positive_real>)` or `time.sleep(<positive_real>)` with positive duration >100ms; any hit is a B-class halt B10 candidate. Path #2 + composite fixtures must use synthetic-clock loops.

11. **AC5.2 + AC5.3 in-process scope qualifier preserved.** Both validation scripts AND the composite test docstrings AND the close-out must explicitly state the in-process scope qualifier ("validates IN-PROCESS LOGIC against SimulatedBroker; does NOT validate IBKR-API timing"). Cessation criterion #5 remains the production-validation gate.

12. **Per-test runtime ≤ 5s (B10).** Run `pytest --durations=10 tests/integration/test_def204_round2_validation.py` and confirm each new test is below 5s.

13. **`SimulatedBrokerWithRefreshTimeout` fixture NOT in S5b.** Verify the fixture variant (and `tests/integration/conftest_refresh_timeout.py`) does NOT appear in S5b's diff — that fixture lands at S5c per Tier 3 item E / DEF-SIM-BROKER-TIMEOUT-FIXTURE. If S5b prematurely added the fixture, this is a scope creep CONCERNS finding.

## Sprint-Level Regression Checklist (for @reviewer)

The full Sprint-Level Regression Checklist is in `docs/sprints/sprint-31.92-def-204-round-2/regression-checklist.md` (34 invariants total).

✓-mandatory at S5b per the Per-Session Verification Matrix:

- **Invariant 1 (DEC-117 atomic bracket):** PASS — production code untouched.
- **Invariant 2 (DEC-364 cancel_all_orders no-args ABC):** PASS.
- **Invariant 3 (DEC-369 broker-confirmed immunity):** PASS — composes additively with `is_reconstructed=True` per AC3.6.
- **Invariant 4 (DEC-372 stop retry caps + backoff):** PASS.
- **Invariant 5 (DEC-385 6-layer side-aware reconciliation):** PASS.
- **Invariant 6 (DEC-386 4-layer OCA architecture):** PASS.
- **Invariant 7 (DEC-388 alert observability):** PASS.
- **Invariant 8 (DEF-158 3-branch side-check verbatim):** PASS — preserved.
- **Invariant 9 (`# OCA-EXEMPT:` mechanism):** PASS.
- **Invariant 10 (test count baseline ≥ 5,269; target +9):** PASS.
- **Invariant 11 (pre-existing flake count):** PASS — DEF-150/167/171/190/192.
- **Invariant 12 (frontend immutability):** PASS.
- **Invariant 13 (SELL-volume ceiling pending+sold pattern):** ✓-mandatory — composite test 5 stresses adversarial load at all 5 emit sites.
- **Invariant 17 (AMD-2 mechanism-conditional):** ✓-mandatory — composite preserves the S2a mechanism choice.
- **Invariant 18 (spike artifacts fresh + committed):** ✓-mandatory — S5b's 2 new artifacts join the freshness checklist.
- **Invariant 19 (`is_reconstructed` refusal posture):** ✓-mandatory — VALIDATES via test 8 + test 9.
- **Invariant 20 (pending-reservation state transitions):** ✓-mandatory — composite re-exercises in integration.
- **Invariant 21 (broker-verification three-branch coverage):** ✓-mandatory — Path #2 fixture exercises three-branch classification.

## Visual Review (N/A — backend-only)

S5b is backend-only. No frontend changes per regression invariant 12.

## Additional Context

- S5b is the second of three validation sessions (S5a + S5b + S5c). It extends S5a's Path #1 validation harness with Path #2 (locate-rejection-with-broker-verified-release), the composite Pytest test producing `sprint-31.92-validation-composite.json` as a Pytest side-effect, the canonical C-2 restart-during-active-position regression test, deferred ceiling-emit-site coverage (S4a-i deferred 4 of 5 sites here), per-`ManagedPosition` ceiling integration, cross-position safety, and reconstruct-from-broker field initialization.
- The composite test produces an artifact at `scripts/spike-results/sprint-31.92-validation-composite.json` whose mtime tracks freshness. The daily CI workflow that runs the test is added by the operator at sprint-close (`doc-update-checklist.md` C9 — NOT in S5b's diff).
- A3 (composite validation produces phantom_shorts > 0 OR `total_sold_le_total_bought: false` OR restart-scenario test fails) halts the sprint. Same failure class that ended Sprint 31.91's `~98%` claim. DO NOT seal the sprint on A3 fire.
- A11 (ceiling false-positive in production): if test 5 (composite-adversarial) fires a ceiling violation on a LEGITIMATE SELL, the implementation is wrong; halt and audit pending-reservation state transitions per AC3.1.
- A15 (restart-scenario test fails): the `is_reconstructed` refusal posture (AC3.7) is the structural defense for the C-2 restart-safety hole. If test 8 fails, the conservative posture is leaking — sprint cannot seal.
- A16 (`is_reconstructed` refusal posture creates operationally undesirable failure mode): conservative refusal has trapped capital. Tier 3 reviews operator-manual flatten infrastructure.
