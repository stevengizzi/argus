# IMPROMPTU-02 (fix) — Bracket amendment leak resolution

> **PLACEHOLDER — this file is generated by the IMPROMPTU-02 scoping session.**
>
> Do not paste this file into a Claude Code session yet. The scoping session at `docs/sprints/sprint-31.9/impromptu-02-scoping.md` will overwrite this placeholder with a complete, ready-to-paste fix prompt based on the root cause it identifies.
>
> Expected timing: after Stage 3 of the Sprint 31.9 campaign (when FIX-04 has landed), the scoping session runs. Once it commits, this file will contain the fix prompt. The fix itself is then scheduled into Stage 9.

## Expected scope (for planning — will be refined by scoping output)

- **Findings resolved:** F-03 (bracket amendment leak), F-04 (flatten retry against non-existent positions), F-10 (emergency flatten frequency)
- **Files likely touched:** `argus/execution/order_manager.py`, possibly `argus/execution/ibkr_broker.py`, tests in `tests/execution/`
- **Safety tag:** `weekend-only` (touches execution layer)
- **Expected DEFs opened + resolved:** 2–3 (bracket-amendment, flatten-invalidation, possibly T2-amendment)
- **Expected DECs:** possibly 1 (if the fix introduces new invalidation semantics around `_broker_confirmed` or moves from "cancel-and-place" to "modify-in-place" via IBKR API)

## Blockers

1. `docs/sprints/sprint-31.9/impromptu-02-findings.md` must exist (produced by the scoping session)
2. FIX-04 must have committed to main
3. Stage 8 (FIX-13 + IMPROMPTU-01) should have completed so the fix lands on a clean, tested main

## When this placeholder is overwritten

The scoping session produces a fix prompt in the same format as the FIX-NN prompts in `docs/audits/audit-2026-04-21/phase-3-prompts/`. Once this file contains real content (not this placeholder), the operator pastes it into a fresh Claude Code session per the normal Sprint 31.9 workflow.
