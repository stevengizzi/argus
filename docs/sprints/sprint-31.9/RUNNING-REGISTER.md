# Sprint 31.9 Running Register

> Canonical snapshot of campaign state: session verdicts, DEF/DEC register,
> outstanding items, deferred pickups, and next-stage plan. Rebuild this file
> at every stage barrier. Survives compaction — read this file to hydrate
> a fresh Claude.ai conversation.
>
> **Last updated:** 2026-04-24 — Apr 24 debrief integration commit. DEF-204 opened (CRITICAL safety — unidentified upstream cascade producing ~14K shares/session of unexpected shorts; A1 fix correctly refused to amplify; mechanism unknown). IMPROMPTU-11 kickoff drafted (diagnostic-only, safe-during-trading). IMPROMPTU-10 SQL-verified working (5-day span all within 7-day retention; no rows to delete — correct behavior). IMPROMPTU-04 validated by Apr 24 live data (44/44 unexpected shorts detected + refused, 86% log reduction, zero doubling, startup invariant present but unexercised). IMPROMPTU-09 kickoff annotated with pre-populated evidence from Apr 24 debrief (gaps VG-1/2/8/9 now have debrief evidence). `post-31.9-reconciliation-drift` named horizon added for DEF-204 fix. SPRINT-CLOSE DISCOVERY stub count raised 3 → 4. P26 retrospective candidate captured in IMPROMPTU-11 kickoff for next campaign's RETRO-FOLD. Campaign HEAD advances with this commit.
> **Campaign HEAD:** *pending* post-integration commit (this commit); prior: `16c049a` RETRO-FOLD+LOW-findings cleanup, `ec7e795` argus submodule bump to metarepo `edf69a5`.
> **Workflow submodule:** `942c53a`
> **Baseline tests:** 5,073 pytest (--ignore=tests/test_main.py) + 39 pass / 5 skip on tests/test_main.py + 866 Vitest (prior: 5057 pass + 859 Vitest post-IMPROMPTU-06)
> **Master plan:** `docs/sprints/sprint-31.9/CAMPAIGN-CLOSE-PLAN.md` — this is the canonical source of truth for Stage 9/10 execution. Read it before acting on any remaining session.

---

## Campaign overview

- **Identifier:** `sprint-31.9-health-and-hardening`
- **Artifacts directory:** `docs/sprints/sprint-31.9/`
- **Key docs:** `README.md`, `STAGE-FLOW.md`, `WORK-JOURNAL-HANDOFF.md`, this register
- **Session count:** 25 planned across 9 stages (Track A: 23 FIX-NN sessions — FIX-13 split into 13a/13b at Stage 8 pre-update; Track B: 2 IMPROMPTU sessions)
- **Operator:** Steven Gizzi (two-Claude workflow: Claude.ai planning + Claude Code implementation, git bridge)
- **Account under test:** IBKR paper U24619949 (paper paused during weekend-only sessions)

---

## Stage status

| Stage | Sessions | Status |
|---|---|---|
| Stage 1 | FIX-00 + FIX-15 + FIX-17 + FIX-20 + FIX-01 + FIX-11 + Stage 1 sweep | ✅ COMPLETE |
| Stage 2 Pass 1 | FIX-02 (config drift via DEC-384 extension) | ✅ CLEAR |
| Stage 2 Pass 2 | FIX-03 (main.py lifecycle — biggest session of campaign to date) | ✅ CONCERNS → RESOLVED in-session |
| **Stage 2 Pass 3** | **FIX-12 + FIX-19 + FIX-21 (parallel)** | ✅ **ALL CLEAR** |
| IMPROMPTU (between 2 and 3) | DEF-172 verify + DEF-173 fix + DEF-175 open | ✅ CLEAR |
| Stage 3 Wave 1 | FIX-14 + FIX-16 | ✅ CLEAR (prior sessions) |
| Stage 3 Wave 2 | FIX-04 (Rule-4 serial, order_manager.py) | ✅ CLEAR |
| Stage 4 Wave 1 | FIX-10 + FIX-18 (parallel) | ✅ CLEAR |
| Stage 4 Wave 2 | FIX-05 solo | ✅ CLEAR |
| **Stage 4** | **(complete)** | **✅ COMPLETE** |
| Stage 5 Wave 1 | FIX-06 (data layer) | ✅ CLEAR |
| Stage 5 Wave 2 | FIX-07 (intelligence/catalyst/quality) | ✅ CLEAR |
| **Stage 5** | **(complete)** | **✅ COMPLETE** |
| Stage 6 | FIX-08 (experiments + learning loop, solo) | ✅ CLEAR |
| **Stage 6** | **(complete)** | **✅ COMPLETE** |
| Stage 7 | FIX-09 (backtest-engine, solo) + IMPROMPTU-03 (CI tz flakes) | ✅ CLEAR / CLEAN |
| **Stage 7** | **(complete)** | **✅ COMPLETE** |
| Stage 8 Wave 1 | FIX-13a (test hygiene — tactical, solo) | ✅ CLEAR |
| Stage 8 Wave 2 | FIX-13b (test hygiene — refactors, solo, 7 findings) | ✅ CONCERNS_RESOLVED |
| Stage 8 Wave 3 | FIX-13c (ai-copilot-coverage, solo, F13 carry-over) | ✅ CLEAR |
| FIX-13a-CI-hotfix | pytest-timeout + 4 WS test patches; DEF-193 opened | ✅ CLEAN (informal) |
| April 22 debrief | DEF-194 through DEF-199 logged; A1 safety bug identified | ✅ Triage committed (053b6f8) |
| **Stage 8** | **(complete; IMPROMPTU-01/02 placeholders re-homed into Stage 9 plan)** | **✅ COMPLETE** |
| Stage 9A | IMPROMPTU-04 (safety: A1 + C1 + startup invariant) | ✅ COMPLETE — commit `0623801`, DEF-199 closed |
| Stage 8.5 | IMPROMPTU-CI (observatory_ws disconnect watcher — DEF-200 + DEF-193) | ✅ CLEAR — DEF-200 opened + closed, DEF-193 closed (shared root cause) |
| Stage 9B | IMPROMPTU-05 (deps & infra — ✅ CLEAR, `6ddd7a7` + review `8d39e21`), IMPROMPTU-06 (test-debt — ✅ CLEAR, `91998bc`; CONCERNS→resolved in-session, DEF-048/049/166/176/185 closed + DEF-192 extended PARTIAL), IMPROMPTU-07 (doc-hygiene + UI — ✅ CLEAR, `add4e83`; DEF-164/169/189/191/198 closed + F-05/F-06/F-08 resolved), IMPROMPTU-08 (architecture.md catalog — ✅ CLEAR, `a905335`; MINOR_DEVIATIONS — 577-line Apr 23 debrief bundled per operator direction; Tier 2 CONCERNS→resolved in-session; DEF-168 closed; 100 REST + 4 WS routes; +4 pytest), IMPROMPTU-10 (evaluation.db retention — ✅ CLEAR, `8bdec82`; CLEAN, zero Tier 2 findings; DEF-197 closed via periodic 4-hour retention task; +3 pytest) | 🚧 PARTIAL |
| Stage 9C | RETRO-FOLD (P1-P25 into workflow/) — ✅ CLEAR; IMPROMPTU-11 (A2/C12 cascade mechanism diagnostic — read-only, DEF-204 mechanism IDENTIFIED) — ✅ DIAGNOSTIC COMPLETE; IMPROMPTU-09 (verification sweep, 9 gaps with 4 pre-populated from Apr 24 debrief) — ⏸ PENDING | 🚧 PARTIAL |
| Stage 10 | SPRINT-CLOSE (summary + seal + archive + 3 DISCOVERY.md) | ⏸ PENDING |

---

## Session history (Stage 1 + Stage 2)

| Session | Commit(s) | Self-assessment | Tier 2 verdict | Test delta | Notes |
|---|---|---|---|---|---|
| FIX-00 (doc-sync obsoletes) | `bac4c06` | MINOR_DEVIATIONS | CLEAR | 0 | Stage 1 |
| FIX-15 (supporting docs) | `9dd44f2` | MINOR_DEVIATIONS | CLEAR | 0 | Stage 1 |
| FIX-17 (.claude/rules refresh) | `451b444` | MINOR_DEVIATIONS | CLEAR | 0 | Stage 1 |
| FIX-20 (sprint runner) | `9737e52` + submodule `942c53a` | MINOR_DEVIATIONS | CLEAR | +0 | Stage 1; disk-full recovery (ENOSPC) |
| FIX-01 (catalyst DB + DEC-384) | `59bb100` | MINOR_DEVIATIONS | CLEAR | +11 | Stage 1; crisis recovery — initial FLAGGED turned out to be stash misread |
| FIX-11 (backend API) | `fc7eb7c` | MINOR_DEVIATIONS | CONCERNS (accepted) | net -2 | Stage 1; lifespan extraction + dev mode retirement |
| Stage 1 sweep | `f3b0464` | CLEAN | CLEAR | 0 | Doc cleanup + DEF logging |
| FIX-02 (overflow.yaml via DEC-384) | `9454df5` + `b23459b` | CLEAN | CLEAR | +3 | Stage 2 Pass 1; first registry extension |
| FIX-03 (main.py lifecycle) | `80af45b` + strikethrough follow-up + `3ad46fa` | MINOR_DEVIATIONS | CONCERNS → RESOLVED | -1 (intentional deletions) | Stage 2 Pass 2; 31 findings, main.py shrunk 2,469 → 2,291 lines |
| FIX-21 (ops cron) | `8ccac67` + `3a6c71d` | CLEAN | CLEAR | 0 (docs-only) | Stage 2 Pass 3 |
| FIX-19 (strategies) | `a2c2512` + `e60cb47` | MINOR_DEVIATIONS | CLEAR | +18 | Stage 2 Pass 3 |
| FIX-12 (frontend) | `db2818b` + `f57a965` | MINOR_DEVIATIONS | CLEAR | Vitest +13 | Stage 2 Pass 3 |
| IMPROMPTU-def172-173-175 | `873738a` | MINOR_DEVIATIONS | CLEAR | +1 | Between Stage 2 and Stage 3; DEF-172 verify-close, DEF-173 code fix, DEF-175 opened + DISCOVERY.md seeded |
| FIX-14 (primary Claude context docs) | `8c36bef` + `f57d7fc` | CLEAN | CLEAR | 0 | Stage 3 Wave 1 |
| FIX-16 (config consistency sweep) | `563ae13` + `942cf05` | MINOR_DEVIATIONS | CLEAR | +19 | Stage 3 Wave 1 |
| FIX-04 (execution layer) | `b2c55e5` + `4cfd8b4` | MINOR_DEVIATIONS | CLEAR | +1 | Stage 3 Wave 2 (Rule-4 serial). 2 CRITICAL + 7 MEDIUM + 10 LOW. Both CRITICALs landed with gold-standard revert-and-fail proof by Tier 2. F11 P1-D1-M03 deferred (DEF-177 — cross-domain RejectionStage edit outside scope); F10 partial (DEF-176 — test-migration blocker). |
| FIX-10 (backtest legacy cleanup) | `675bf78` + `3efac5a` | CLEAN | CLEAR | 0 (docs-only) | Stage 4 Wave 1 (parallel with FIX-18). 3 findings, trivial. |
| FIX-18 (deps + infra hardening) | `7aabb96` + `5fe4d1d` | MINOR_DEVIATIONS | CLEAR | 0 | Stage 4 Wave 1 (parallel with FIX-10). 15 findings (2 CVE + 9M + 4L + 2 cosmetic). CI workflow (`.github/workflows/ci.yml`) introduced. Cleanup tracker #2 + #3 RESOLVED. |
| HOTFIX pytest-xdist | `d261e7b` + `a896985` | — | — | 0 | Post-FIX-18: CI surfaced missing `pytest-xdist` in `[dev]` extras. Declared + annotated FIX-18 follow-up. |
| HOTFIX clean-install | `793d4fd` | — | — | 0 | First full CI run unmasked 4 clean-install bugs (submodule init, seaborn for report generator, jwt→jose shim in one test, walk-forward integration marking). All fixed. |
| FIX-05 (core: orchestrator + risk + regime) | `4590859` + `2fec7ca` + `f0283d3` | CLEAN | CLEAR | +10 | Stage 4 Wave 2. 37 findings (2 CRITICAL + 18 MEDIUM + 17 LOW). Both CRITICALs landed with regression tests exercising uncovered lines. Closed DEF-091/092/104/163/170. Opened DEF-182 (weekly reconciliation). |
| FIX-06 (data layer) | `4ea09a7` + `49fef3b` | MINOR_DEVIATIONS | CLEAR | +17 | Stage 5 Wave 1. 26 findings (1 CRITICAL + 8 MEDIUM + rest LOW/COSMETIC). F25 CRITICAL resolved via FIX-16-compatible overlay path (spec option (b) moot post-FIX-16); three-layer revert-proof regression defense. SystemAlertEvent added to core/events.py as documented scope expansion. DEF-037/165 closed; DEF-014 PARTIAL (emitter side landed; HealthMonitor subscription awaits P1-A1 M9); DEF-032 re-verified; DEF-183 opened (Alpaca retirement). |
| FIX-07 (intelligence/catalyst/quality) | `7b70390` | MINOR_DEVIATIONS | CLEAR | +12 | Stage 5 Wave 2 (serial). 23 findings (7 MEDIUM + rest LOW, no CRITICAL). Finding 5 (RejectionStage split) deferred to DEF-184, coordinated with DEF-177. Two scope expansions ratified: `argus/core/protocols.py` (new file, FIX-06 precedent) + `argus/intelligence/learning/models.py` (actual DEF-106 location; spec cited wrong file). DEC-311 received Amendment 1 (kept[-1] dedup anchor pinned via option (c) — preserves in-flight paper-trading counts). DEF-096 + DEF-106 closed; DEF-184 opened. |
| FIX-08 (experiments + learning loop) | `33ad7da` + `daae0fd` | MINOR_DEVIATIONS | CLEAR | +7 | Stage 6 (solo). 18 findings (3 MEDIUM + 7 LOW + 8 COSMETIC, no CRITICAL). Finding 1 (fingerprint unification runner↔factory) landed with detection-only hash pinned to `ddec1b2a09ee2263` (backward-compat verified). Finding 15 (redistribution drift recording) closes the cumulative-drift-guard evasion loophole. Finding 17 (threshold raise/lower split) eliminates the contradictory-proposal bug. DEF-107 + DEF-123 closed. Spec path drift on Finding 18 documented (P12). Reviewer surfaced DEF-163 re-open — Python-side fix from FIX-05 was incomplete (SQL-side UTC/ET date mismatch in TradeLogger.get_todays_pnl). |
| FIX-09 (backtest-engine) | `f639a98` + `caab644` | MINOR_DEVIATIONS | CLEAR | −56 | Stage 7 (solo). 27 findings — no CRITICAL; 3 MEDIUM + 9 LOW + 9 COSMETIC + 3 MEDIUM test findings + retirement cluster. 3 major file retirements landed cleanly: `report_generator.py` (operator-approved Option A, 1,232 LOC + 578 tests), `vectorbt_pattern.py` (1,057 LOC + ~50 tests), `vectorbt_red_to_green.py` (~1,025 LOC + ~60 tests, chained). F14 walk-forward R2G branch excised. F2 `itertuples` dispatch cleared 15% gate by 5.8× (+87.8%). F1 flat-key fallback dropped with added WARNING log. F9 NYSE holiday filter wired. DEF-186 + DEF-187 opened. Tier 2 reviewer surfaced pyarrow/xdist race (→ DEF-190 this barrier) and prior-session bug in `scripts/revalidate_strategy.py:383` (→ DEF-189 this barrier). Audit back-annotation used per-file summary headers rather than per-row strikethrough — reviewer confirmed format equivalence. |
| IMPROMPTU-03 (CI tz flakes) | `354be7f` | CLEAN | (no Tier 2; micro-session) | +1 | Stage 7 closing. Resolved post-FIX-09 CI failures: DEF-163 (re-opened at Stage 6) and DEF-188 (new, discovered post-FIX-09). Both test-side bugs only — implementations correct (ET-based); tests had wrong timezone assumptions. DEF-163 fix: replaced `exit_time=now` with fixed 15:00 ET anchor (market hours — avoids SQL-side UTC-normalization window 20:00–24:00 ET; production trades never exit in that window). DEF-188 fix: compare against ET-derived today instead of local-tz `date.today()`. DEF-191 opened for latent `get_todays_pnl` SQL-side concern (post-31.9; matters only if ARGUS ever supports after-hours trading). CI verification at 22:59 ET on 2026-04-22 inside the failing window = P14 regression evidence satisfied. |
| FIX-13a (test hygiene — tactical) | `c9c8891` + `f06fd18` | MINOR_DEVIATIONS | CLEAR | +7 | Stage 8 Wave 1 (solo). 25 findings total; 15 tactical RESOLVED, 4 RESOLVED-VERIFIED (F17/F20/F22/F24 — two were stale audit observations), 1 PARTIAL (F25), 8 DEFERRED to FIX-13b. Zero new DEFs opened per kickoff carve-out. DEFs closed: DEF-150 (time-of-day arithmetic fix), DEF-167 (3 named Vitest files converted to dynamic dates; remaining ~55 mock-fixture files have no decay surface per kickoff scope), DEF-171 (ULID xdist race — `id(order) % 10000` replaced with `itertools.count(1)` at `test_ibkr_broker.py:91`), DEF-190 (pyarrow/xdist race — conftest prewarm via Period-dtype DataFrame→Arrow conversion; regression guard test added). DEF-192 PARTIAL: numpy invalid-cast cleanly closed at 3 sites in `vectorbt_afternoon_momentum.py`; 5 other categories deferred (TestBaseline blocked on workflow submodule per RULE-018). F19 narrowing exposed latent Sprint 25 silent-failure in route-disabled test (broad `except Exception: pass` was swallowing `pytest.fail()`). F6 kickoff-error caught by Claude Code (SIMULATED → IBKR correction). Mid-session RULE-018 catch-and-revert preserved workflow submodule integrity. |
| FIX-13b (test hygiene — refactors) | `18a73d7` + `edcc626` + `3f8cfc7` + `9d27909` + `d9d3fe2` + `d329856` + `0352a85` + `4aebeb5` + `b7de595` | MINOR_DEVIATIONS | CONCERNS_RESOLVED | ±0 | Stage 8 Wave 2 (solo, 7 findings). All 7 deferred findings from FIX-13 split landed cleanly: F5 `_build_system()` real `ArgusSystem.__init__`, F7+F8 6 sprint-dated integration files moved to `tests/integration/historical/` (sprint13 moved-not-deleted per in-session triage — `TestBrokerSourceConfig` exercises live surfaces), F9 6 flatten tests monkeypatch `eod_flatten_timeout_seconds=0.1s` (~63s runtime savings; FIX-04 had already reduced 4 of 6 to ~1s so the kickoff's 180s claim was partially stale), F11 13 order-manager test files under `tests/execution/order_manager/` subpackage (4 `parents[2]→parents[3]` path fixes), F18 `_make_trade` helper extracted (228→105 LOC), F21 `monitor_poll_seconds` constructor param on AlpacaDataService (production default 5.0 preserved), F23 `orb_config_factory` callable-fixture replaces `make_orb_config` (35 call-site migration; callable pattern chosen over `.model_copy(update=...)` due to nested sub-models at 6 call sites). Zero new DEFs opened, zero behavior change, 4,987 → 4,987 pytest. F13 (AI Copilot coverage) DEFERRED to FIX-13c per kickoff plan. One production file touched: `argus/data/alpaca_data_service.py` (F21 injection). Zero workflow/ changes. |
| FIX-13c (ai-copilot-coverage) | `fe64ad2` | CLEAN | CLEAR | +52 | Stage 8 Wave 3 (solo, Finding 13 from FIX-13 split). Pure test-only session, zero production code touched, zero new test files, zero conftest.py changes. 4 AI Copilot modules lifted from baseline coverage to ≥ 85% line: prompts.py 56% → 99.4% (5 untested page formatters parametrized + non-string content path), context.py 64% → 86.1% (System page body + system state error paths), client.py 71% → 95.6% (retry logic + import guard + streaming error + send_with_tool_results), executors.py 69% → 88.5% (GenerateReportExecutor.execute all 3 branches + RiskParamChangeExecutor remaining param paths + registry.register). 72 → 124 tests in `tests/ai/` (+52). Two lines deliberately left uncovered as defensively unreachable (prompts.py:95 integer-division dead branch, client.py:267 retry-loop fallback with hardcoded max_retries=3) — both mathematically verified, reporting-not-patching per Halt Condition 3. Kickoff's "+25 to +35" estimate undercounted by ~70% due to parametrized test fan-out (5 cases per @parametrize) and exhaustive error-path expansion. Every added test anchored to an enumerated gap line. Single consolidated commit per kickoff option. |
| FIX-13a-CI-hotfix (pytest-timeout + WS test patches) | `d6bdf87` + `7573ea4` + `1141e56` + `0d58ad9` | CLEAN | (informal verification — CI green at green URL) | 0 | Diagnostic + fix for CI timeout regression first manifesting with FIX-13a (c9c8891). Root cause: observatory WS push-only loop doesn't detect client disconnect on Linux under xdist (latent pre-existing bug, unmasked by FIX-13a test-env timing shift). Test-only fix: added pytest-timeout permanently (120s global + 30s on ws tests), ws_update_interval_ms=200 on 4 tests, extra ws.receive_json() in graceful_disconnect. Production-side bug logged as DEF-193 (post-31.9). |
| IMPROMPTU-04 (DEF-199 EOD short-flip + C1 log + startup invariant) | `0623801` | MINOR_DEVIATIONS | CONCERNS (CI-gate) | +13 | Stage 9A (Track B, safety-critical). Three fixes bundled: (A1) side-check at `order_manager.py:1707` + `:1684` — BUY flattens, SELL/unknown logs ERROR + skips; (C1) `pattern_strategy.py:318` log level INFO → DEBUG (Apr 22: 87% of log volume); (invariant) new `check_startup_position_invariant()` helper + `_startup_flatten_disabled` flag in `main.py`, gates `reconstruct_from_broker()` on any non-BUY side. 6 revert-proof DEF-199 canaries + 5 invariant helper tests + 2 C1 log-level tests. Pre-existing Sprint 32.9 / 29.5 EOD tests updated to declare `side=OrderSide.BUY` on mock positions (attribute previously absent). DEF-199 RESOLVED; DEF-194/195/196 remain open as causal upstream. Tier 2 verdict CONCERNS awaiting CI-green (unblocked by IMPROMPTU-CI). |
| IMPROMPTU-CI (observatory_ws disconnect watcher — DEF-200 + DEF-193) | `a50ac8d` | CLEAR (`4cccf90`) | CLEAR (`224d773`) | +2 | Stage 8.5 (inserted between Stage 9A and Stage 9B to unblock campaign P25 green-CI rule). Production fix at `argus/api/websocket/observatory_ws.py:116-148`: `asyncio.Event` disconnect sentinel + background `_watch_disconnect()` task that awaits `websocket.receive()`; push loop now races the interval via `asyncio.wait_for(_disconnect_event.wait(), timeout=interval_s)` and exits promptly on peer close. Watcher cancelled + drained in handler `finally`. DEF-200 opened for the specific CI surfacing of the bug (`test_observatory_ws_sends_initial_state` crashed xdist worker on commits `150da6b`/`053b6f8`/`c655cb3`/`2290401`); closed in the same session. DEF-193 closed as a same-root-cause sibling (identical CI signature on `test_observatory_ws_independent_from_ai_ws`). Two regression tests at `tests/api/test_observatory_ws.py`: (a) `test_observatory_ws_has_disconnect_watcher_sentinel` — grep-guard, revert-proof on every platform; (b) `test_observatory_ws_disconnect_cancels_push_loop_promptly` — behavioral, 5s push interval, <2s cleanup budget, revert-proof on Linux/xdist only (macOS TestClient cancels cleanly pre-fix). Net delta +2 pytest. Zero production changes outside `observatory_ws.py`. |
| IMPROMPTU-05 (deps & infra — DEF-179 + DEF-180 + DEF-181) | `6ddd7a7` | CLEAR (`80d041c`) | CLEAR (`8d39e21`) | 0 | Stage 9B (Track B, safe-during-trading). Four independent cleanups bundled: (DEF-179) `python-jose` → `PyJWT` migration at 5 production sites (`argus/api/auth.py`, `argus/api/websocket/{live,observatory_ws,ai_chat,arena_ws}.py`) + 3 test sites (`tests/api/test_auth.py` 3 inline imports, `tests/execution/order_manager/test_safety.py:511`, `tests/api/test_fix11_backend_api.py` 2 comment refs); `pyproject.toml`: `python-jose[cryptography]>=3.4.0,<4` → `PyJWT>=2.8,<3`; exception handlers `JWTError` → `PyJWTError`; grep audit returns zero `from jose` / `import jose` matches. (DEF-180) `uv`-generated lockfiles landed — `requirements.lock` (188 packages, runtime only) + `requirements-dev.lock` (262 packages, `dev` + `backtest` extras); `.github/workflows/ci.yml` install step replaced with `pip install -r requirements-dev.lock && pip install -e . --no-deps`; new `docs/deps.md` documents regen recipe. Lockfile byte-reproducible across regens. (DEF-181) GitHub Actions pins bumped to Node-24-compatible majors: `checkout@v4→v6`, `setup-python@v5→v6`, `setup-node@v4→v6` (all verified Node 24 via release notes). (LaCie) `CANDIDATE_CACHE_DIRS` in `populate_historical_cache.py` pruned to local-only. Zero test delta by design; 55 JWT-related tests pass; full suite 5054 → 5054. |
| IMPROMPTU-06 (test-debt & warning-cleanup — ...) | `91998bc` | MINOR_DEVIATIONS | CONCERNS→resolved in-session (2 findings addressed pre-seal: DEF-049 row weakened to "full-file isolation + -n auto" with Tier 2 note; skip-reason plural fixed in code "21 → 5") | +3 (--ignore=tests/test_main.py) + 16 (test_main.py) | Stage 9B (Track B, safe-during-trading). Seven cleanups bundled: (DEF-176) legacy `OrderManager(auto_cleanup_orphans=...)` kwarg + DeprecationWarning guard + fallback removed; 3 reconciliation test files migrated to `ReconciliationConfig` (1 helper rewrite + 1 test rename). (DEF-185) 5 `assert isinstance` sites → `raise TypeError` guards across `analytics/ensemble_evaluation.py` × 3 and `intelligence/learning/outcome_collector.py` × 2; 5 revert-proof regression tests added. (DEF-166) close-with-verification — `test_speed_benchmark` confirmed already replaced by `test_backtest_and_replay_produce_equivalent_results` at `test_walk_forward_engine.py:562`. (DEF-048/049) 4 structural test_main.py fixture fixes identified: api.enabled: false, exit_management.yaml creation, broker.get_positions AsyncMock, tmp_path data_dir — collapsed 21 → 5 failures; residual 5 xdist flakes skipped with explicit DEF-048 reason decorators. DEF-049 target passes clean in both isolated and -n auto. (DEF-192) categories (iii) websockets.legacy + (iv) OrderManager deprecation + (iv.b) PyJWT InsecureKeyLengthWarning all ELIMINATED via targeted `pyproject.toml` filterwarnings + DEF-176 landing; category (ii) AsyncMock-never-awaited reduced (test_timeout_enforcement `_close_and_timeout` side_effect); warnings 43 → 26 (-40%). (Non-DEF) TestOverflowConfigYamlAlignment deleted (2 vacuous tests). Files modified: 9; files added: 2; files deleted: 0. test_main.py state: 23 pass / 21 fail → 39 pass / 5 skip / 0 fail under both isolated and -n auto. Net pytest delta on --ignore branch: +3. |
| IMPROMPTU-07 (doc-hygiene + UI — DEF-164 + DEF-169 + DEF-189 + DEF-191 + DEF-198 + Apr-21 F-05/F-06/F-08 + cosmetics X1–X6 + shadow-variant badge + CLAUDE.md variant-count note) | `add4e83` | CLEAR | CLEAR (`add4e83` + cleanup `489cb3f`) | +16 pytest / +7 Vitest | Stage 9B (Track B, safe-during-trading). Eleven cleanups bundled: (DEF-164) `OrderManagerConfig.auto_shutdown_boot_grace_minutes: int = 10` + `_boot_monotonic` capture at top of `start()` + `_on_shutdown_requested` grace deferral + 4 regression tests. (DEF-189) `_PARAM_NAME_MAP` + `_translate_params()` in `revalidate_strategy.py` with Pydantic `model_fields` validation; flat-key overrides replace silently-no-op dot-prefix form + 8 regression tests (contaminated revalidation re-run deferred to Sprint 33). (DEF-198) path (b) — grep-verified actual 19-phase boot sequence (12 primary + 7 sub-phases); `architecture.md:1199` + 3 `FIX-03-closeout.md` rows + `start()` docstring corrected; `[N/12]` labels NOT renumbered per kickoff guidance. (DEF-191) doc-only NOTE block on `get_todays_pnl()` SQLite UTC quirk. (DEF-169) strikethrough reclassification citing FIX-11 commit `fc7eb7c`. (F-05) ULID truncation 8→12 chars at 2 sites + 2 regression tests. (F-06) additive REST enrichment with `mfe_r`/`mae_r` on `/counterfactual/positions` (dollar fields preserved; SQLite schema untouched); TS interface + ShadowTradesTab bindings + 2 pytest + 1 Vitest tests. (F-08) `logger.warning`→`logger.debug` at `risk_manager.py:622`. (X1–X6) 6 pure-archaeology inline comments removed from `main.py`. (Shadow-variant badge) structural `__` delimiter `stripVariantSuffix()` helper + all 5 accessor helpers variant-resolve to base (no name-pattern heuristic) + 6 Vitest tests. (CLAUDE.md) variant-count methodology note added. Files modified: 20; files added: 3. Scope boundaries respected: workflow/ untouched, audit docs untouched, CounterfactualPosition SQLite schema untouched, no `revalidate_strategy.py` re-run executed. CI run 24860817762 green. |
| IMPROMPTU-08 (architecture.md API catalog regeneration — DEF-168) | `a905335` | MINOR_DEVIATIONS (577-line Apr 23 debrief bundled per operator direction; not reflected in close-out manifest) | CONCERNS→resolved in-session pre-commit (F1: `test_verify_helper_detects_drift` rewritten to call `verify_catalog_freshness()` rather than reimplement detection; F2: stale DEF-168 HTML comment at `architecture.md:156-163` updated to cite closure) | +4 pytest (all in new `tests/docs/` module) | Stage 9B (Track B, safe-during-trading). `scripts/generate_api_catalog.py` (455 LOC, reads `app.openapi()`, falls back to `@<router>.websocket(...)` decorator scan) regenerates 9 catalog sections from the authoritative schema: §4 primary (by tag, 26 tags), §4 WebSocket + stream-payload narrative, §4 Control (5 routes), §4 Orchestrator (4 incl. previously-undocumented `override-throttle`), §7.9 AI REST (10), §13.5.1 Arena REST + 390→720 bar-cap refresh (Sprint 32.8), §15.8 Experiments + Counterfactual (8 incl. previously-missing `/counterfactual/accuracy`). §7.8 WS JSON schemas preserved. §14.2 renamed "Comparison Module API" — pure Python module, not route-exposed; signatures via `inspect.signature()`. Catalog freshness meta-note at top of §4. Pre-fix `--verify` listed 33 missing paths; post-fix exits 0. 4 regression tests (all-REST drift-probed + confirmed revert-proof; all-WS coverage; generator idempotence; `verify_catalog_freshness()` sanity). Zero runtime code changed (`git diff argus/` empty). |
| IMPROMPTU-10 (evaluation.db retention — DEF-197 priority elevated MEDIUM→HIGH per Apr 23 trajectory) | `8bdec82` | CLEAN | CLEAR (`2c9a564`) | +3 pytest | Stage 9B (Track B, safe-during-trading). DEF-197 pulled forward from post-31.9-component-ownership per +94.6% single-session growth trajectory (9,294 MB Apr 23 vs 4,776 MB Apr 22; live DB at session-start was 14,549 MB). Kickoff at `docs/sprints/sprint-31.9/IMPROMPTU-10-evaluation-db-retention.md`. **Diagnostic confirmed:** Q1 returned `MIN/MAX trading_date = 2026-04-20 / 2026-04-23` (4 distinct days, all within 7-day retention window), 61M rows, freelist 0.0% — startup `cleanup_old_events()` was a no-op because no rows matched the cutoff yet, but at ~5 GB/day ingestion a multi-day session crosses the boundary and accumulates day-8+ rows until next reboot. **Fix candidate (1):** `EvaluationEventStore.initialize()` now spawns `_run_periodic_retention()` (`asyncio.create_task`, `RETENTION_INTERVAL_SECONDS = 4*60*60`); `close()` cancels and awaits the task; per-iteration exceptions caught + logged. `RETENTION_DAYS=7` and the fire-and-forget write pattern unchanged. 3 regression tests in `tests/test_telemetry_store.py` (start-on-init, cancel-on-close, periodic-fires-cleanup). One-shot operator cleanup (`VACUUM INTO data/evaluation.db.new` + swap) documented in IMPROMPTU-10-closeout.md §7, not committed. DEF-192 warning count ~25–27 across 3 runs (baseline 26) — within documented xdist variance, no new category introduced. |
| RETRO-FOLD (P1–P25 → `claude-workflow` metarepo) + LOW-findings follow-on | argus: `aa952f9`+`48bea1b`+`204462e`+`d4f0ef0`+`ec7e795` / metarepo: `63be1b6`+`ac3747a`+`edf69a5` | CLEAN | CLEAR (`c8dfc37`, 2 LOW findings folded into `edf69a5` same doc-sync pass) | N/A (doc-only, no argus code) | Stage 9C (cross-repo — the one campaign session where RULE-018 does not apply by design). 25 P-lessons landed in metarepo: +13 RULE entries (RULE-038–RULE-050) in `claude/rules/universal.md`; +4 to `claude/skills/close-out.md` (P4/P10/P11/P25); +3 to `claude/skills/review.md` (P1/P3/P25); +3 to `protocols/sprint-planning.md` (P13/P20/P23); +2 to `templates/implementation-prompt.md` (P2/P5). **Consolidation:** P6/P12/P13/P19/P22 → RULE-038 with 4 sub-variants (pre-session grep-verify, spec path drift, tracker-nickname reconciliation, audit-count staleness); per-P# traceability preserved in CAMPAIGN-COMPLETENESS-TRACKER (landed in `48bea1b`). **Verification:** 25 Origin footnotes grep-confirmed across 5 metarepo files. Submodule pointer advanced `942c53a` → `63be1b6` → `ac3747a` → `edf69a5`. **No new files** created in metarepo (`bootstrap-index.md` unchanged). **No tag** created (metarepo has no prior tag convention). Zero argus code/test/config/audit modifications. **Tier 2 verdict:** CLEAR with 2 LOW-severity Origin-footnote findings; both folded in same doc-sync pass: (1) RULE-042 — added explicit note that dict.get extension is same-shape generalization currently ungrounded (to be grounded when a future dict.get-default bug surfaces); (2) RULE-045 — replaced "DEF-190's neighbors" (pyarrow/xdist race, unrelated) with DEF-167 (Vitest hardcoded-date decay, actual timezone-family sibling). Rule bodies unchanged in both cases. |
| IMPROMPTU-11 (A2/C12 cascade mechanism diagnostic — DEF-204) | *pending commit* | *pending close-out* | *pending Tier 2* | 0 pytest (read-only) | Stage 9C (Track B, safe-during-trading). **Diagnostic-only session — zero code changes** (`git diff argus/ tests/ config/` empty). Investigated the Apr 24 debrief's 44-symbol / 14,249-share gradual-drip cascade independent of DEF-199 with **mechanism IDENTIFIED to high-confidence**. 8 hypotheses evaluated (the kickoff's required 6 + 2 emergent: H7 redundant-standalone-SELL + H8 reconcile-call-site-strips-side). IMSR detailed case study at 16:17 UTC traced through three brackets — bracket 2 the dominant leak (escalation-stop fill 200 + trail-flatten partial 100 racing → broker -100; DEF-158 retry doubled to -200 via `abs(qty)` side-blind read of broker SHORT). **Mass-balance evidence** (2,225 broker fills − 899 entries − 679 ARGUS-recognized exits = ~647 unaccounted SELL fills routed to silent `logger.debug("Fill for unknown order_id ...")` at `order_manager.py:592`) supports H1 as ~98% of blast radius. Top-3 ranking: #1 H1+H7 (no `ocaGroup` on bracket children + standalone trail/escalation SELLs not in OCA group) — primary fill-side mechanism; #2 H2+H8 (reconcile only handles ARGUS-orphan direction + call site strips side info) — primary detection blindness allowing 6-hour silent accumulation; #3 H5 (DEF-158 retry path side-blind, doubles pre-existing shorts). P26 retrospective candidate captured ("validate fix against mechanism signature, not symptom aggregate" — origin Apr 24 debrief). DEF-204 OPEN — fix scope routed to **`post-31.9-reconciliation-drift`** named-horizon sprint, ~3 sessions: (a) explicit `ocaGroup`+`ocaType=1` on bracket children + thread `oca_group_id` through ManagedPosition; (b) side-aware reconciliation contract `dict[str, float]` → `dict[str, tuple[OrderSide, int]]` + handle broker-orphan direction; (c) side-aware DEF-158 retry path mirroring IMPROMPTU-04 EOD pattern. Diagnostic at `docs/sprints/sprint-31.9/IMPROMPTU-11-mechanism-diagnostic.md`. **Pre-existing test failures observed** (not caused by this session, confirmed via `git stash`): `tests/intelligence/test_filter_accuracy.py` 11 failures + `tests/api/test_counterfactual_api.py` 1 failure — root cause is hardcoded `opened_at: str = "2026-03-25T10:00:00"` at `tests/intelligence/test_filter_accuracy.py:36` falling outside the rolling 30-day default window now that today is 2026-04-24 (date-decay sibling of DEF-167 family). Out of scope for this read-only diagnostic session. |

Baseline progression: 4,934 (pre-campaign) → 4,858 (actual pytest at campaign start after FIX-03's CLAUDE.md strikethrough) → 4,944 (post-FIX-11) → 4,946 (post-FIX-02) → 4,964 (post-Stage-2) → 4,965 (post-IMPROMPTU-def172-173-175) → 4,984 (post-FIX-16) → 4,985 (post-FIX-04, holds through Stage 4 Wave 1 + hotfixes) → 5,000 (post-FIX-05) → 5,017 (post-FIX-06) → 5,029 (post-FIX-07) → 5,035 (post-FIX-08) → 4,979 (post-FIX-09, −56 sanctioned deletion delta) → 4,980 (post-IMPROMPTU-03, +1 minor drift) → 4,987 (post-FIX-13a, +7 tactical test additions) → 4,987 (post-FIX-13b, ±0 pure refactor) → 5,039 (post-FIX-13c, +52 AI coverage expansion) → 5,052 (post-IMPROMPTU-04, +13 DEF-199 safety tests) → 5,054 (post-IMPROMPTU-CI, +2 DEF-200 regression guards) → 5,054 (post-IMPROMPTU-05, 0 delta) → 5,057 (post-IMPROMPTU-06, +5 DEF-185 type-guard tests minus −2 TestOverflowConfigYamlAlignment deletion) → 5,073 (post-IMPROMPTU-07, +16 across DEF-189/DEF-164/F-05/F-06 regression tests) → 5,077 (post-IMPROMPTU-08, +4 freshness tests in new `tests/docs/` module) → **5,080 (post-IMPROMPTU-10, +3 periodic-retention lifecycle tests in `tests/test_telemetry_store.py`)**. test_main.py tracked separately: 23 pass → 39 pass + 5 skip post-IMPROMPTU-06 (unchanged by IMPROMPTU-07/08/10). Vitest: 859 → 866 (post-IMPROMPTU-07, +7 shadow-variant badge + F-06 column binding tests) → 866 (unchanged by IMPROMPTU-08/10, no UI surface).

---

## DEF register (sprint-campaign-scoped items)

### Resolved this campaign

| DEF # | Description | Session | Commit |
|---|---|---|---|
| DEF-034 | `SessionResult.review_verdict` Pydantic warning | FIX-20 | `9737e52` |
| DEF-048 + 049 | `test_main.py` env leak (autouse fixture applied; remaining isolation failure is pre-existing, out of DEF-046 pattern scope) | FIX-03 | `80af45b` |
| DEF-074 | Dual regime recheck path consolidation | FIX-03 | `80af45b` |
| DEF-082 | `CatalystStorage` pointed at `argus.db` instead of `catalyst.db` | FIX-01 | `59bb100` |
| DEF-093 | `main.py` duplicate orchestrator YAML load + `_latest_regime_vector` typing | FIX-03 | `80af45b` |
| DEF-097 | Monthly cache update cron (`populate_historical_cache.py --update`) | FIX-21 | `8ccac67` |
| DEF-142 | `quality_engine.yaml` / `system*.yaml` drift | FIX-01 (via DEC-384) | `59bb100` |
| DEF-162 | Monthly re-consolidation cron (`consolidate_parquet_cache.py --resume`) — paired with DEF-097 | FIX-21 | `8ccac67` |
| DEF-172 | Duplicate `CatalystStorage` instances — RESOLVED-VERIFIED (behavioral): dual close paths both fire; SQLite WAL enables safe concurrent reads; structural dedup deferred to DEF-175 | IMPROMPTU-def172-173-175 | `873738a` |
| DEF-173 | `LearningStore.enforce_retention()` never called — RESOLVED: wired in `argus/api/server.py::_init_learning_loop` mirroring FIX-03's ExperimentStore pattern; +1 regression test | IMPROMPTU-def172-173-175 | `873738a` |
| DEF-091 | Public accessors for V1 RegimeClassifier + VIXDataService private attrs — `compute_trend_score()` + `vol_low_threshold`/`vol_high_threshold` properties on V1; `config` property on VIXDataService | FIX-05 | `4590859` |
| DEF-092 | Unused Protocol types in `argus/core/regime.py` — four orphaned Protocol classes deleted | FIX-05 | `4590859` |
| DEF-104 | Dual ExitReason enums drift risk — `argus.core.events.ExitReason` now re-exports from `argus.models.trading` (single source of truth) | FIX-05 | `4590859` |
| DEF-170 | VIX regime calculators inert in production — `RegimeClassifierV2.attach_vix_service()` re-instantiates all four VIX calculators from the injected service | FIX-05 | `4590859` |
| DEF-032 | `criteria_list` parameter ignored on FMP scanner — RESOLVED-VERIFIED: DEF-032 still accurate; inline pointer comment added at call site | FIX-06 | `4ea09a7` |
| DEF-037 | FMP API key redaction in error logs — `_redact()` helper threaded through 4 FMP network-error log sites | FIX-06 | `4ea09a7` |
| DEF-165 | DuckDB conn close hang when CREATE VIEW interrupted — `self._conn.interrupt()` before `.close()` | FIX-06 | `4ea09a7` |
| DEF-096 | Protocol type for duck-typed candle store + store references — `argus/core/protocols.py` with `CandleStoreProtocol` + `CounterfactualStoreProtocol` | FIX-07 | `7b70390` |
| DEF-106 | `models.py from_dict()` assert isinstance batch — 8 sites in `intelligence/learning/models.py` + 1 in `routes/counterfactual.py` converted to `if not isinstance: raise TypeError` | FIX-07 | `7b70390` |
| DEF-107 | Unused `raiseRec` destructured variable in LearningInsightsPanel.tsx L388 — deleted | FIX-08 | `33ad7da` |
| DEF-123 | `build_parameter_grid()` float accumulation — RESOLVED-VERIFIED: existing `round(min + i*step, 6)` form is mathematically equivalent to `numpy.arange`; spec-suggested numpy migration would add a dependency for cosmetic gain; docstrings updated with rationale | FIX-08 | `33ad7da` |
| DEF-163 | Timezone-boundary bug in `test_get_todays_pnl_excludes_unrecoverable` — SQLite `date()` UTC-normalization + synthetic `exit_time=now` during 20:00–24:00 ET window | IMPROMPTU-03 | `354be7f` |
| DEF-188 | `test_market_calendar::test_defaults_to_today` compared ET-based implementation result to local-tz `date.today()` | IMPROMPTU-03 | `354be7f` |
| DEF-150 | Time-of-day arithmetic bug in `test_check_reminder_sends_after_interval` — `(minute - 2) % 60` broke for minutes 0 and 1 | FIX-13a | `c9c8891` |
| DEF-167 | Vitest hardcoded-date decay — 3 test files converted to `new Date().toISOString()` or dynamic `Date.now()` offsets (TradesPage, PerformancePage, ResearchDocCard). Remaining ~55 Vitest files have hardcoded date strings but all are non-assertion fixture data with no decay surface (closed-with-scope-note per FIX-13a deferred_observations). | FIX-13a | `c9c8891` |
| DEF-171 | ULID xdist race — `id(order) % 10000` produced collisions on low 13 bits under xdist's worker forking. Replaced with fixture-scoped `itertools.count(1)` in `test_ibkr_broker.py:91`. | FIX-13a | `c9c8891` |
| DEF-190 | pyarrow/xdist concurrent `register_extension_type` race — conftest prewarm via `pd.DataFrame({"_p": [Period('2024-01')]}) → pa.Table.from_pandas(df)` forces extension registration at module-import time before xdist forks workers. Regression guard at `tests/test_def190_pyarrow_eager_import.py`. | FIX-13a | `c9c8891` |
| DEF-199 | `_flatten_unknown_position()` doubled short positions at EOD (Apr 22 paper session: 50 of 51 untracked broker positions ended exactly 2× short, 34,239 shares). Three-part fix: (a) side-check branch at `order_manager.py:1707` (EOD Pass 2); (b) same three-branch logic at `:1684` (EOD Pass 1 retry) via new `broker_side_map`; (c) `check_startup_position_invariant()` in `main.py` with `_startup_flatten_disabled` flag gating `reconstruct_from_broker()`. `ibkr_broker.py` + `models/trading.py` + `_flatten_unknown_position` implementation all untouched. +13 pytest: 6 revert-proof A1 canaries, 5 invariant helper tests, 2 C1 log-level tests. Pre-existing Sprint 32.9 / 29.5 mocks updated to declare `side=OrderSide.BUY`. Cross-references DEF-194/195/196 (causal upstream — remain open). | IMPROMPTU-04 | `0623801` |
| DEF-200 | `test_observatory_ws_sends_initial_state` crashed its xdist worker on Linux CI (`aiosqlite._connection_worker_thread` → `call_soon_threadsafe()` → `RuntimeError: Event loop is closed`). Root cause: push loop at `argus/api/websocket/observatory_ws.py` was `while True: await asyncio.sleep(interval_s)` — never called `receive()`, so the server task stayed parked past TestClient disconnect. Observed on CI commits `150da6b`, `053b6f8`, `c655cb3`, `2290401` (3+ consecutive docs-only commits). Fix: `asyncio.Event` disconnect sentinel + `_watch_disconnect()` background task + `asyncio.wait_for(_disconnect_event.wait(), timeout=interval_s)` race against the interval; watcher cancelled + drained in handler `finally`. **Same fix closes DEF-193** — identical CI failure signature on the same commit's `test_observatory_ws_independent_from_ai_ws` proved shared root cause. Regression guards: grep-guard `test_observatory_ws_has_disconnect_watcher_sentinel` (revert-proof on every platform; asserts fix markers) + behavioral `test_observatory_ws_disconnect_cancels_push_loop_promptly` (5s push interval, <2s cleanup budget — revert-proof on Linux/xdist only because macOS TestClient cancellation succeeds pre-fix). | IMPROMPTU-CI | `a50ac8d` |
| DEF-193 | Observatory WebSocket push-only loop doesn't detect client disconnect on Linux (same root cause as DEF-200 — closed together). | IMPROMPTU-CI | `a50ac8d` |
| DEF-179 | `python-jose` → `PyJWT` migration across 5 production sites (`argus/api/auth.py`, `argus/api/websocket/{live,observatory_ws,ai_chat,arena_ws}.py`) + 3 test sites (`tests/api/test_auth.py` 3 inline imports, `tests/execution/order_manager/test_safety.py`, `tests/api/test_fix11_backend_api.py` comment refs). `pyproject.toml`: `python-jose[cryptography]>=3.4.0,<4` → `PyJWT>=2.8,<3`. Exception handlers updated `JWTError` → `PyJWTError`. No `.decode('utf-8')` call sites found. Grep audit `grep -rn "from jose\|import jose" argus/ tests/` returns zero. 55 JWT-related tests pass. | IMPROMPTU-05 | `6ddd7a7` |
| DEF-180 | Python lockfile via `uv` — `requirements.lock` (188 packages, runtime only) + `requirements-dev.lock` (262 packages, `dev` + `backtest` extras). `.github/workflows/ci.yml` install step now `pip install -r requirements-dev.lock && pip install -e . --no-deps` with `cache-dependency-path` → `requirements-dev.lock`. New `docs/deps.md` documents regen recipe, install instructions, reproducibility notes. Second regen produces byte-identical output apart from filename-in-header comment. `incubator` extra excluded (pairs with DEF-178). | IMPROMPTU-05 | `6ddd7a7` |
| DEF-181 | GitHub Actions pins bumped to Node-24-compatible majors: `actions/checkout@v4→v6`, `actions/setup-python@v5→v6`, `actions/setup-node@v4→v6`. All three verified via `gh api repos/actions/{name}/releases/latest` (checkout@v6.0.2, setup-python@v6.2.0, setup-node@v6.4.0). Hard deadline 2026-06-02 (Node 20 forcible upgrade) met with 40 days of margin. | IMPROMPTU-05 | `6ddd7a7` |
| DEF-048 | test_main.py xdist failures — root cause was stale mocks, not the load_dotenv/AIConfig race the DEF description asserted. Four structural fixes: (1) `api.enabled: false` added to the base fixture YAML + the 5 per-test overrides that recreate system.yaml; (2) `exit_management.yaml` added to the base fixture (required by Phase 10 `load_yaml_file`); (3) `broker.get_positions = AsyncMock(return_value=[])` added to all mock setup loops (required by IMPROMPTU-04's DEF-199 startup invariant check); (4) fixture `data_dir` pointed at `tmp_path / "data"` (absolute) to isolate from the repo's 13 GB `data/evaluation.db`. Collapsed 21 isolated failures → 5 xdist-residual flakes; those 5 skipped with explicit `@pytest.mark.skip(reason="DEF-048: xdist-only flake. IMPROMPTU-06 reduced test_main.py failures 21 → 5; these 5 remain under -n auto.")`. State: 39 pass / 5 skip / 0 fail under both isolated and `-n auto`. | IMPROMPTU-06 | `91998bc` |
| DEF-049 | `test_orchestrator_uses_strategies_from_registry` isolation failure — same root-cause family as DEF-048 (api.enabled + exit_management.yaml + get_positions AsyncMock fixture gaps). Test now passes in both isolation AND `-n auto` AND the full suite. No skip required. | IMPROMPTU-06 | `91998bc` |
| DEF-166 | `test_speed_benchmark` flaky under pytest-cov — close-with-verification. Grep confirms zero test-function matches; the functional-equivalence replacement at `tests/backtest/test_walk_forward_engine.py:562` (`test_backtest_and_replay_produce_equivalent_results`) already supersedes it per its own docstring, which explicitly cites itself as "Functional-equivalence replacement for the former test_speed_benchmark." The DEF was a stale observation; FIX-09 P1-G1-M01 / P1-G2-M03 had already resolved the flake. | IMPROMPTU-06 | `91998bc` |
| DEF-176 | Legacy `OrderManager(auto_cleanup_orphans=...)` kwarg removed. `test_reconciliation.py` helper migrated to typed `ReconciliationConfig(auto_cleanup_orphans=...)` routed via `reconciliation_config=` parameter; `test_reconciliation_redesign.py::test_legacy_auto_cleanup_orphans_still_works` renamed to `test_reconciliation_config_auto_cleanup_orphans_true` with preserved behavioral assertion; `test_sprint2875.py` was already using the typed config form. Then `auto_cleanup_orphans: bool = False` parameter + docstring entry + `DeprecationWarning` guard + `ReconciliationConfig(auto_cleanup_orphans=...)` fallback all deleted from `OrderManager.__init__`. Unused `import warnings` removed. Post-fix grep-audit confirms zero legacy-kwarg call sites. | IMPROMPTU-06 | `91998bc` |
| DEF-185 | 5 `assert isinstance` sites converted to `if not isinstance: raise TypeError(...)`: `argus/analytics/ensemble_evaluation.py:189` (data_range list), `:196` (marginal_contributions dict), `:204` (baseline_ensemble dict); `argus/intelligence/learning/outcome_collector.py:209` (exit_time str), `:303` (closed_at str). 5 revert-proof regression tests added at `tests/analytics/test_ensemble_evaluation_type_guards.py` (3 tests, `pytest.raises(TypeError, match=...)`) and `tests/intelligence/learning/test_outcome_collector_type_guards.py` (2 tests, `patch.object(oc_module, "dict", bad_dict)` + `exc_type is TypeError` assertion on captured WARNING exc_info). Restoring `assert` surfaces AssertionError, failing the tests. | IMPROMPTU-06 | `91998bc` |
| DEF-164 | Late-night boot vs EOD auto-shutdown collision — `OrderManagerConfig.auto_shutdown_boot_grace_minutes: int = 10` (ge=0; 0 disables). `ArgusSystem._boot_monotonic` captured at the top of `start()`; `_on_shutdown_requested` defers with INFO log if `(monotonic() - _boot_monotonic) < grace * 60`. Protects the full Phase-11 HistoricalQueryService CREATE VIEW window from DEF-165 interrupt-close hangs. Regression: `tests/core/test_boot_grace.py` (4 tests — deferral, post-grace proceed, grace=0 disable, boot_monotonic=None defensive). | IMPROMPTU-07 | `add4e83` |
| DEF-169 | `--dev` mode retired (FIX-11, commit `fc7eb7c`) — RESOLVED-VERIFIED; row promoted from INFORMATIONAL to strikethrough in CLAUDE.md. `_mock_watchlist` now test-only; `GET /trades/{id}/replay` → 501 until DEF-029 lands (separate scope). | IMPROMPTU-07 | `add4e83` |
| DEF-189 | `scripts/revalidate_strategy.py:383` config_overrides param-name mismatch — RESOLVED. `_PARAM_NAME_MAP` table translates VectorBT names to Pydantic field names per strategy (`or_minutes → orb_window_minutes`, `target_r → target_2_r`, `max_hold_minutes → time_stop_minutes`, `volume_multiplier → volume_confirmation_multiplier`, `time_stop_bars → time_stop_minutes`/`max_hold_minutes`). New `_translate_params()` helper validates every translated key against `model_fields` on the target Pydantic config; unknown keys filtered out with WARNING. `{yaml_name}.{k}` dot-prefix dropped in favour of flat keys. Bug fix only — contaminated revalidations re-run scoped to Sprint 33 Statistical Validation. Regression: `tests/scripts/test_revalidate_strategy.py` (8 tests). | IMPROMPTU-07 | `add4e83` |
| DEF-191 | Latent `TradeLogger.get_todays_pnl()` SQL-side UTC normalization — RESOLVED-DOC-ONLY. Inline NOTE block added to `argus/analytics/trade_logger.py::get_todays_pnl` documenting the UTC-normalization quirk, the market-hours-only safety envelope, and fix options for a future after-hours-trading sprint. No code change; no regression test (doc comment only). | IMPROMPTU-07 | `add4e83` |
| DEF-198 | Boot phase labels `[N/12]` vs FIX-03 handoff's "17-phase" claim — RESOLVED-VERIFIED (path (b) — doc correction). Grep confirms actual `argus/main.py` boot sequence is 12 primary + 7 sub-phases (7.5, 8.5, 9.5, 10.25, 10.3, 10.4, 10.7) = 19 phases total; original "17" miscounted 9.5 and 10.4. Corrected `docs/architecture.md:1199`, three `FIX-03-closeout.md` rows, and `argus/main.py::start()` docstring ("five config-gated sub-phases" → "seven sub-phases — 19 phases total"). `[N/12]` labels not renumbered per IMPROMPTU-07 kickoff guidance (cosmetic + error-prone). | IMPROMPTU-07 | `add4e83` |
| Apr-21 F-05 | ULID log-truncation width widened 8→12 chars at `argus/analytics/trade_logger.py:126` (`log_trade`) and `:648` (`log_orchestrator_decision`). Regression: `tests/analytics/test_trade_logger.py::TestF05LogTruncationWidth` (2 tests). Retires "duplicate trade_id" debug confusion. | IMPROMPTU-07 | `add4e83` |
| Apr-21 F-06 | Shadow Trades MFE/MAE unit mismatch — RESOLVED via REST-response enrichment (option (a) per debrief). `_enrich_with_r_multiples()` in `argus/api/routes/counterfactual.py` computes `mfe_r`/`mae_r` at serialization time; MAE flipped to negative R. Dollar fields preserved for backward-compat. `ShadowTrade` TS interface extended; `ShadowTradesTab.tsx` column cells + sort keys bind `mfe_r`/`mae_r` (old `max_*_excursion` sort keys removed). Regression: `tests/api/test_counterfactual_api.py::TestF06MfeMaeRMultiples` (2 tests) + `ShadowTradesTab.test.tsx::test_f06_mfe_mae_r_columns_render_from_r_multiple_fields` (1 test). | IMPROMPTU-07 | `add4e83` |
| Apr-21 F-08 | `PRIORITY_BY_WIN_RATE is not fully implemented` warning downgraded to DEBUG at `argus/core/risk_manager.py:622` with inline comment explaining the IMPROMPTU-07 rationale (known-unfinished feature; 100+/session pollution drowned genuine alerts). | IMPROMPTU-07 | `add4e83` |
| Shadow-variant badge | `argus/ui/src/utils/strategyConfig.ts` — new `stripVariantSuffix()` helper parses the `__` structural separator in experiment-variant IDs. `getStrategyDisplay` + `getStrategyBorderClass` + `getStrategyBarClass` + `getStrategyBadgeClass` + `getStrategyColor` all fall back to the base strategy's display config when the full variant ID isn't in `STRATEGY_DISPLAY`. Badge colors now inherit from the base strategy instead of rendering as greyed-out "unknown". Regression: 7 new tests in `strategyConfig.test.ts` under `describe('experiment-variant strategy IDs')`. | IMPROMPTU-07 | `add4e83` |
| Cosmetic X1–X6 | 6 pure "# Sprint X: <name>" inline archaeology comments deleted from `argus/main.py` (lines 211 `universe_manager`, 219 `eval_check_task`, 224 `bg_refresh_task`, 228+229 `counterfactual_tracker`+`counterfactual_store`, 231 `promotion_evaluator`). Remaining Sprint-era comments have current context ("closed on shutdown", "reused in health check", "counter for INFO logging cadence", etc.) — preserved per "err on the side of keeping anything that provides current context." | IMPROMPTU-07 | `add4e83` |
| DEF-168 | `docs/architecture.md` API catalog drift — RESOLVED. `scripts/generate_api_catalog.py` reads `app.openapi()` (100 REST routes) + scans `argus/api/websocket/*.py` for `@<router>.websocket(...)` decorators (4 WebSocket routes). All 9 catalog sections regenerated from the authoritative schema: §4 primary (by tag, 26 tags), §4 WebSocket + stream-payload narrative, §4 Control (5 routes), §4 Orchestrator (4 routes — `override-throttle` added), §7.9 AI REST (10), §13.5.1 Arena REST (verified), §15.8 Experiments + Counterfactual (8, adds `/counterfactual/accuracy`). §7.8 WS narrative + §14.2 "Comparison Module API" (renamed — not route-exposed, signatures via `inspect.signature()`). Catalog freshness note at top of §4. Freshness gate: `python scripts/generate_api_catalog.py --verify` + `tests/docs/test_architecture_api_catalog_freshness.py` (4 tests — all-REST path match, all-WS path match, generator idempotence, verify-detects-injected-route). Pre-fix `--verify` listed 33 missing paths; post-fix reports OK. | IMPROMPTU-08 | TBD |

### Partially resolved

| DEF # | Description | Status | Owner |
|---|---|---|---|
| DEF-014 | SystemAlertEvent for dead data feed — PARTIAL: emitter side wired at `databento_data_service._run_with_reconnection()` with max-retries-exceeded emission; HealthMonitor subscription + Command Center alert-pane surface await P1-A1 M9 expansion. 3 additional TODO emitter sites at `ibkr_broker.py:453,531` and `alpaca_data_service.py:593` remain (execution/data layer, cross-domain). | PARTIAL (FIX-06 emitter side) | P1-A1 M9 (subscription) + execution-layer session (remaining emitters) |
| DEF-192 | Test runtime warning cleanup debt. Baseline was 39 warnings across ~6 categories. **PARTIAL-EXTENDED (IMPROMPTU-06, 2026-04-23):** further closures — (iii) `websockets.legacy` deprecation ELIMINATED via `[tool.pytest.ini_options].filterwarnings` in `pyproject.toml` (grep-audit confirms zero direct websockets imports; warning is transitive via uvicorn[standard]); (iv) `OrderManager(auto_cleanup_orphans=...)` deprecation ELIMINATED via DEF-176 kwarg removal + test migration; (iv.b) new PyJWT `InsecureKeyLengthWarning` (surfaced by IMPROMPTU-05's migration on a 29-byte test secret) ELIMINATED via same filterwarnings stanza; (ii) AsyncMock coroutine-never-awaited REDUCED — one recurring site at `test_timeout_enforcement` fixed via `_close_and_timeout(coro, ...)` side_effect that closes the unawaited coroutine before raising TimeoutError; remaining ~3-5 sites are intermittent and xdist-order-dependent. Final count 43 → 26 warnings (-17, -40%). Still-remaining categories: (i) aiosqlite "Connection was deleted before being closed" ResourceWarning (~24 sites across production long-lived connection pattern — architectural, not a leak; fixing requires broad test-fixture teardown refactor; accepted DEF-192 debt); (v) `TestBaseline` pytest collection warning in `workflow/scripts/sprint_runner/state.py` — submodule, Universal RULE-018 prohibits in-project edits. Prior FIX-13a contribution (numpy invalid-cast in `vectorbt_afternoon_momentum.py`) remains in place. | PARTIAL-EXTENDED (IMPROMPTU-06; categories iii + iv + iv.b eliminated) | Opportunistic / next test-hygiene touch — categories (i) aiosqlite + (v) TestBaseline remaining |

### Open with planned owner

| DEF # | Description | Priority | Owner |
|---|---|---|---|
| ~~DEF-168~~ | ~~`docs/architecture.md` API catalog drift~~ — **RESOLVED** IMPROMPTU-08 | ~~LOW~~ | ~~P1-H1a~~ — ✅ LANDED |
| ~~DEF-169~~ | ~~`--dev` mode retired (informational only)~~ — **RESOLVED-VERIFIED** IMPROMPTU-07 | ~~INFO~~ | ~~Ongoing~~ — ✅ LANDED |
| DEF-174 | Tauri desktop wrapper never integrated; `platform.ts` deleted as misleading dead code | LOW / opportunistic | Deferred (only if desktop packaging becomes a requirement) |
| DEF-175 | Component ownership consolidation — `CatalystStorage`, `SetupQualityEngine`, `DynamicPositionSizer`, `ExperimentStore`, `LearningStore` constructed in both `main.py` and `api/server.py` lifespan phases; broader pattern behind DEF-172/173 | MEDIUM | **Dedicated post-Sprint-31.9 sprint** (~2–3 sessions). Pre-sprint discovery at `docs/sprints/post-31.9-component-ownership/DISCOVERY.md`. Blocked on Sprint 31.9 closure. |
| ~~DEF-176~~ | ~~Full removal of deprecated `OrderManager(auto_cleanup_orphans=...)` kwarg~~ — **RESOLVED** IMPROMPTU-06 | ~~LOW~~ | ~~Opportunistic~~ — ✅ LANDED |
| DEF-177 | `RejectionStage.MARGIN_CIRCUIT` — FIX-04 P1-D1-M03 deferred; requires cross-domain edit (intelligence/counterfactual.py enum + counterfactual_positions schema + order_manager.py:485 emitted stage) that exceeded FIX-04's execution-only scope | MEDIUM | **Dedicated cross-domain session, must coordinate with DEF-184** (both want to modify the same RejectionStage enum in orthogonal directions) |
| DEF-178 | `alpaca-py` still in core `[project.dependencies]` despite DEC-086 demoting Alpaca to incubator-only — FIX-18 left constraint in place with inline pointer; full fix moves to `[project.optional-dependencies].incubator` + feature-detect at 4 call sites | LOW | Opportunistic / execution-layer cleanup sprint |
| ~~DEF-179~~ | ~~`python-jose` → `PyJWT` migration~~ — **RESOLVED** IMPROMPTU-05 | ~~LOW~~ | ~~Opportunistic~~ — ✅ LANDED |
| ~~DEF-180~~ | ~~No Python lockfile~~ — **RESOLVED** IMPROMPTU-05 | ~~LOW-MEDIUM~~ | ~~Dedicated single-session~~ — ✅ LANDED |
| ~~DEF-181~~ | ~~Node 20 deprecation in GitHub Actions~~ — **RESOLVED** IMPROMPTU-05 | ~~LOW~~ | ~~Before 2026-06-02~~ — ✅ LANDED (40 days of margin) |
| DEF-182 | Weekly reconciliation full implementation — `HealthMonitor._run_weekly_reconciliation()` has been a placeholder since Sprint 5; FIX-05 upgraded log level and pointed at this DEF. Full fix needs broker `get_order_history(days=7)` pairing with `TradeLogger.get_trades_by_date_range(...)` + discrepancy alerts. | LOW | Opportunistic / operations sprint |
| DEF-183 | Full Alpaca code+test retirement — delete `alpaca_data_service.py`, `alpaca_scanner.py`, associated tests, and config branches; simplify `main.py:301-317` / `:339-346` to a single live path. Pairs with DEF-178 (dependency-removal half). | LOW | Opportunistic / execution-layer cleanup sprint |
| DEF-184 | `RejectionStage` → `RejectionStage` + `TrackingReason` split — shadow-mode and overflow routing aren't really "rejections." Current `RejectionStage.SHADOW` appears in `FilterAccuracy.by_stage` breakdowns as a rejection category, which is semantically wrong. Touches the enum, FilterAccuracy cut logic, REST serialization on `/counterfactual/accuracy`, `counterfactual_positions.rejection_stage` SQLite schema, and every `SignalRejectedEvent(stage=SHADOW)` emission site. | MEDIUM | **Dedicated cross-domain session, must coordinate with DEF-177** (both want to modify RejectionStage in orthogonal directions) |
| ~~DEF-185~~ | ~~Analytics-layer `assert isinstance` anti-pattern (DEF-106 follow-on)~~ — **RESOLVED** IMPROMPTU-06 | ~~LOW~~ | ~~Opportunistic~~ — ✅ LANDED |
| DEF-186 | BacktestEngine private-attribute reach-in consolidation (SimulatedBroker._pending_brackets + PatternBasedStrategy._pattern + RiskManager/OrderManager event_bus type-ignore sites) | LOW | post-31.9 (next execution/backtest cleanup) |
| DEF-187 | Migrate walk-forward IS path from VectorBT to BacktestEngine (retire walk_forward.py + 4 vectorbt_*.py files, ~6,713 LOC + ~4,108 test LOC) | MEDIUM | Sprint 33+ (validation-tooling sprint) |
| ~~DEF-189~~ | ~~`scripts/revalidate_strategy.py:383` config_overrides param-name mismatch~~ — **RESOLVED** IMPROMPTU-07 | ~~MEDIUM~~ | ~~post-31.9~~ — ✅ LANDED |
| ~~DEF-191~~ | ~~Latent `TradeLogger.get_todays_pnl()` SQL-side UTC normalization~~ — **RESOLVED-DOC-ONLY** IMPROMPTU-07 | ~~LOW~~ | ~~post-31.9~~ — ✅ LANDED (doc-only; code change deferred to after-hours-trading sprint) |
| DEF-193 | Observatory WS push-only loop doesn't detect client disconnect on Linux | MEDIUM | post-31.9-component-ownership sprint |
| DEF-194 | IBKR `ib_async` stale position cache after reconnect (PURR 1.00× outlier Apr 22) | MEDIUM | post-31.9-reconnect-recovery-and-rejectionstage sprint |
| DEF-195 | `max_concurrent_positions` diverges from broker state; 8% BITO concentration bypass | HIGH-live / MEDIUM-paper | post-31.9-reconnect-recovery-and-rejectionstage sprint |
| DEF-196 | 32 DEC-372 stop-retry-exhaustion cascade after IBKR reconnect (9:40–9:59 ET Apr 22) | MEDIUM | post-31.9-reconnect-recovery-and-rejectionstage sprint |
| ~~DEF-197~~ | ~~`data/evaluation.db` 4.78 GB at boot on Apr 22; 9,294 MB on Apr 23; 14,549 MB at session start. Retention DELETE matched zero rows because all data was within the 7-day window — single startup-only call couldn't keep up with multi-day sessions.~~ — **RESOLVED** IMPROMPTU-10 (periodic 4-hour retention task spawned from `EvaluationEventStore.initialize()`; cancelled in `close()`; +3 lifecycle regression tests). One-shot `VACUUM INTO` cleanup is an operator step documented in IMPROMPTU-10-closeout.md §7, not committed code. | ~~HIGH~~ | ~~IMPROMPTU-10~~ — ✅ LANDED |
| ~~DEF-198~~ | ~~Boot phase labels `[N/12]` contradict FIX-03 handoff's claimed 17-phase sequence~~ — **RESOLVED-VERIFIED** IMPROMPTU-07 (path (b): handoff doc correction, actual count is 19) | ~~LOW~~ | ~~IMPROMPTU-07~~ — ✅ LANDED |
| ~~DEF-199~~ | ~~`_flatten_unknown_position()` systematically doubles short positions at EOD~~ | **CRITICAL** | ✅ **RESOLVED** — IMPROMPTU-04 commit `0623801` |
| DEF-202 | Post-shutdown process hang (63 min, IBKR reconnect-after-STOPPED, task destruction storm). Apr 23 debrief §C9. Subsumes Apr 22 §C7. Cross-refs DEF-175, DEF-193, DEF-201. | MEDIUM | post-31.9-component-ownership sprint |
| DEF-203 | `max_concurrent_positions` WARNING spam not throttled. 10,729 events on Apr 23 (vs 8,996 Apr 22). Apr 23 debrief §C10. | LOW | Category 3 MONITOR-ONLY — next `risk_manager.py` touch |
| DEF-204 | CRITICAL SAFETY — upstream cascade of unexpected shorts (44 symbols/14,249 shares on Apr 24); unidentified mechanism producing gradual reconciliation-mismatch drift. Independent of DEF-199; Apr 24 debrief §A2+§C12 | CRITICAL | IMPROMPTU-11 (diagnostic, Sprint 31.9) → `post-31.9-reconciliation-drift` (fix, post-campaign) |

---

## DEC register (this campaign)

| DEC # | Description | Session(s) | Status |
|---|---|---|---|
| DEC-384 | `load_config()` standalone YAML overlay (Option B); `_STANDALONE_SYSTEM_OVERLAYS` registry in `argus/core/config.py` | FIX-01 (landed), FIX-02 (first extension with `overflow.yaml`) | ✅ LANDED |

---

## Outstanding code-level items (not DEF-tracked)

Items too small/cosmetic to promote to DEF, but worth surfacing at the appropriate session kickoff:

| Item | Location | Severity | Target pickup |
|---|---|---|---|
| `TestOverflowConfigYamlAlignment` no-op after system.yaml overflow removal | `tests/intelligence/test_config.py` | INFO | FIX-13 test-hygiene (Stage 8) |
| RSK-NEW-5 dangling reference in two AI module comments | `argus/ai/conversations.py`, `argus/ai/usage.py` | LOW | FIX-07 intelligence (Stage 5) — candidate |
| ~~Cosmetic X1–X6 (sprint/DEC/AMD archaeology comments)~~ — **RESOLVED** IMPROMPTU-07 | ~~`argus/main.py`~~ | ~~INFO~~ | ✅ LANDED (6 comments deleted; 11 with current-context preserved) |
| `tests/test_main.py` stale mocks (DEF-049 isolation — pre-existing, covers older subsystems) | `tests/test_main.py` | INFO | Broader refresh unlikely before FIX-13 |
| ~~`populate_historical_cache.py` `CANDIDATE_CACHE_DIRS` LaCie legacy entries~~ — removed IMPROMPTU-05 | ~~`scripts/populate_historical_cache.py:70-75`~~ | ~~INFO~~ | ✅ RESOLVED |
| ~~Shadow-variant badge rendering (`strat_dip_and_rip__v2_*`/`v3_*` still greyed out)~~ — **RESOLVED** IMPROMPTU-07 via delimiter-based base-strategy lookup in `strategyConfig.ts`; +7 Vitest | ~~`argus/ui/src/utils/strategyConfig.ts` + `Badge.tsx`~~ | ~~LOW~~ | ✅ LANDED |
| 5 remaining DEF-138 rejection-reason labels (`chase_protection`, `volume_insufficient`, `quality_below_threshold`, `terminal_state`, `max_positions`) | Various strategy files | LOW | Sprint 33+ observability |
| M03/M05/M06 frontend primitive migration (~35 useQuery callers / ~10 error-UI sites / ~15 skeleton sites) | `argus/ui/src/` | INFO | Opportunistic during future page touches |
| Audit spec-prompt file-path drift posture (FIX-12 had `.ts` vs `.tsx` systemic drift; FIX-19 had `Throughout` + wildcard entries) | `docs/audits/audit-2026-04-21/phase-3-prompts/` | INFO | Ongoing — verify targets in every kickoff |

---

## Campaign-wide process notes and learnings

**Parallel execution risk profile (Pass 3 findings)**
1. **c3bc758 chimera incident.** During Pass 3, FIX-19's session momentarily created an intermediate commit that swept FIX-12's working-tree edits under a FIX-19 label. The session reset it locally before pushing; never touched origin. Outcome clean, but margin was thinner than desired. Future parallel batches should add a hard pre-commit `git diff --name-only --cached` check against declared scope.
2. **Working-tree noise contaminates pytest re-runs during review.** FIX-21's reviewer ran pytest 3 times with (1, 15, 13) failures respectively, all explained by concurrent uncommitted FIX-19 test work. Resolution: `git stash -u` + re-run → clean. For campaign post-mortem: reviewer skills should default to `git stash -u` before pytest when working tree is dirty, restore after.
3. **Rule of thumb:** Docs-only sessions parallelize freely. Code sessions on shared high-value paths must serialize. File-disjoint code sessions can parallelize with caveats above.

**Kickoff prompt hygiene**
- **Path drift:** Audit prompts predate some file reorganizations (FIX-20 `models.py` → `state.py`; FIX-12 `.ts` vs `.tsx`; FIX-19 "Throughout" + wildcard entries). Always grep-verify file paths before editing.
- **Baseline staleness:** Every FIX-NN prompt says "Expected baseline: 4,934". Campaign has moved past this baseline repeatedly; kickoffs must correct with current HEAD baseline.
- **DEF naming collisions:** Audit uses DEF-NNN labels internally that may or may not correspond to CLAUDE.md-tracked DEFs. FIX-19's "DEF-138 scope" is audit-internal; CLAUDE.md's DEF-138 is a different Sprint 32.8 item. Kickoffs should disambiguate explicitly.

**Review mechanism reliability**
- FIX-03 got CONCERNS verdict exclusively because reviewer grep-verified CLAUDE.md DEF-074/093 strikethroughs that close-out claimed were present. This catch is load-bearing; all future sessions must grep-verify DEF strikethroughs before commit.
- Tier 2 review catches what Tier 1 self-review misses. Keep the pair.

**Structural patterns to preserve**
- FIX-01's DEC-384 registry pattern (`_STANDALONE_SYSTEM_OVERLAYS` in `argus/core/config.py`) is the canonical standalone-overlay pattern. Extend via tuple entry + flat bare-fields YAML (not wrapped). FIX-02 was the first extension.
- FIX-12's helper-export pattern (`getStrategyLetter(id)`, `getStrategyShortName(id)`) over consumer-side derivation is the canonical single-source-of-truth pattern for frontend config consolidation.
- FIX-19's legacy alias layer (`STRATEGY_LEGACY_ALIASES` in `strategyConfig.ts`) is the canonical pattern for supporting old bare short IDs (`'orb'` → `'strat_orb_breakout'`) without hardcoding a second map.

---

## Stage 3 complete (2026-04-22)

All three Stage 3 sessions landed cleanly. Executed in two waves:
- **Wave 1 (parallel):** FIX-14 (docs) + FIX-16 (config) — both CLEAR
- **Wave 2 (solo Rule-4 serial):** FIX-04 (execution) — CLEAR with gold-standard revert-and-fail proof

Plus the IMPROMPTU-def172-173-175 session between Stage 2 and Stage 3 (DEF-172 RESOLVED-VERIFIED, DEF-173 RESOLVED, DEF-175 opened for post-31.9 sprint).

Test progression: 4,965 → 4,984 (FIX-16 +19) → 4,985 (FIX-04 +1). Vitest unchanged at 859.

## Stage 4 Wave 1 complete (2026-04-22)

FIX-10 + FIX-18 landed in parallel, both CLEAR. FIX-18 introduced the first `.github/workflows/ci.yml`, which triggered two hotfix cycles:

1. **`d261e7b` + `a896985`** — CI run surfaced that `pytest-xdist` was missing from `[dev]` extras (local envs had it via system pip; clean install did not). Declared in `pyproject.toml`, FIX-18 close-out annotated.
2. **`793d4fd`** — First fully passing CI after clean-install bugs: (a) git submodule init missing, (b) seaborn missing for report generator import, (c) one `jwt` import shim missing in a test under jose-3.5 line, (d) walk-forward validation needed `@pytest.mark.integration` marking.

## CI infrastructure status

First fully passing CI run achieved at commit `793d4fd`:

- **pytest:** 4,977 passing + 0 failing + 0 errors (CI runs with `-m "not integration"`)
- **Vitest:** 859 passing + 0 failing
- **Known-flake DEFs:** none fired (DEF-150, DEF-163 × 2, DEF-167, DEF-171 — all four dormant)
- **Workflow file:** `.github/workflows/ci.yml` (added by FIX-18 P1-I-M06)
- **Clean-install bugs unmasked and fixed:** pytest-xdist in `[dev]`, submodule init, seaborn, jwt-import shim, walk-forward integration marking
- **Known deprecation warning:** Node 20 on three GitHub Actions — tracked as **DEF-181** (June 2, 2026 deadline)

## Stage 4 complete (2026-04-22)

Stage 4 sealed with FIX-05 Wave 2 CLEAR. Both waves landed with zero scope violations:
- **Wave 1 (parallel):** FIX-10 (backtest legacy, 0 tests) + FIX-18 (deps + CI workflow, +0 tests, two hotfix cycles)
- **Wave 2 (solo):** FIX-05 (core orchestrator/risk/regime, +10 tests, 5 DEFs closed, 1 opened)

FIX-05 closed DEF-091, DEF-092, DEF-104, DEF-163 (Python-side), and DEF-170 via `attach_vix_service()` re-instantiation. DEF-182 (Weekly reconciliation stub) opened as an opportunistic operations-sprint item.

Post-Stage-4 expected CI baseline: 4,992 pytest (+15 over 793d4fd's 4,977 after FIX-05's +10 + FIX-16's +19 minus integration filter adjustments) + 859 Vitest.

## Stage 5 preview (FIX-06 + FIX-07 next)

Next up: FIX-06 (data layer) + FIX-07 (intelligence layer). See the master tracker at `docs/sprints/sprint-31.9/CAMPAIGN-COMPLETENESS-TRACKER.md` for the full remaining-session + DEF matrix.

---

## Snapshot maintenance protocol

This document is the external survival buffer for the campaign state. **Update at every stage barrier** (ideally as the last step before closing a stage):

1. Edit this file in `docs/sprints/sprint-31.9/RUNNING-REGISTER.md`
2. Update `Last updated`, `Campaign HEAD`, baseline tests at top
3. Update the Stage status table
4. Add rows to Session history for any new sessions
5. Move DEFs between register sections as status changes
6. Commit with message `docs(sprint-31.9): update running register — <stage>`

The goal is that anyone (or any fresh Claude.ai conversation) can read this file and know exactly where the campaign stands without reading every prior close-out and review document.

**Hydration pattern for a fresh Claude.ai conversation:** paste this file contents + attach the most recent FIX-NN close-out and review — that plus project knowledge is enough to resume.

**For Stage 9/10 campaign-close work specifically:** paste `CAMPAIGN-CLOSE-PLAN.md` instead of the most recent close-out. That doc carries the full Loop-Closure Matrix + session plan + reboot instructions for the remaining 8 sessions. The plan doc is the authoritative source for what to do next; this register is the operational state snapshot.
