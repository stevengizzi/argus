# Audit: Frontend
**Session:** P1-F2
**Date:** 2026-04-21
**Scope:** `argus/ui/src/` — 402 `.ts`/`.tsx` files across 10-page Command Center, sampling + pattern audit
**Files examined:** ~35 deep / ~80 skimmed (sampled across each of 10 pages, all WS hooks, all strategy-identity call sites, Vitest config, router)

---

## CRITICAL Findings

| # | File:Line | Finding | Impact | Suggested Fix | Safety |
|---|-----------|---------|--------|---------------|--------|
| C1 | [utils/strategyConfig.ts:29-126](argus/ui/src/utils/strategyConfig.ts#L29-L126) + [components/Badge.tsx:16,28-45,83-120](argus/ui/src/components/Badge.tsx#L16) | **RESOLVED FIX-12-frontend** — The three Sprint 31A strategies — `strat_micro_pullback`, `strat_vwap_bounce`, `strat_narrow_range_breakout` — are NOT registered in either source of strategy identity. Both `STRATEGY_DISPLAY` (strategyConfig.ts) and `strategyColors`/`strategyLabels`/`strategyLetters` (Badge.tsx) stop at 12 strategies. Live universe has 15. | Any trade, signal, or Arena card from these strategies renders with the grey fallback (`#6b7280`) and an auto-title-cased name like "Micro Pullback"/letter "M"/shortName "MICR". Visually they look like unknown/unrecognized strategies instead of first-class ones. Affects Dashboard tables, Trade Log badges, Arena cards, Performance breakdowns, Orchestrator signal rows — everywhere a strategy_id is rendered. The 2 Dip-and-Rip shadow variants (`strat_dip_and_rip__v2_*`/`__v3_*`) also fall through to the same grey path. | Add three entries to `STRATEGY_DISPLAY`, `STRATEGY_BORDER_CLASSES`, `STRATEGY_BAR_CLASSES`, and the three `Badge.tsx` maps. Pick unused Tailwind hues (e.g., indigo, fuchsia, green). While at it, resolve M1 (single source of truth) so this regression is impossible going forward. | safe-during-trading |

## MEDIUM Findings

| # | File:Line | Finding | Impact | Suggested Fix | Safety |
|---|-----------|---------|--------|---------------|--------|
| M1 | [components/Badge.tsx:28-120](argus/ui/src/components/Badge.tsx#L28) vs [utils/strategyConfig.ts:29-163](argus/ui/src/utils/strategyConfig.ts#L29) | **RESOLVED FIX-12-frontend** — Two independent sources of truth for strategy identity. `Badge.tsx` hardcodes `strategyColors`, `strategyLabels`, `strategyLetters` for 12 strategies; `strategyConfig.ts` duplicates the same metadata for the same 12 with its own color hexes and Tailwind class lists. No imports between them. The maps already disagree — e.g., `Badge.tsx` prints `"MOM"` for afternoon_momentum, `strategyConfig.ts` prints `"PM"`. | Silent drift. Any color/label change requires simultaneous edits in both files; missing one produces inter-page inconsistency (see M2). Root cause of C1 — each new strategy needs to be added in 2 places. | Pick `strategyConfig.ts` as canonical. Refactor `Badge.tsx` `strategyColors`/`strategyLabels`/`strategyLetters` to derive from `STRATEGY_DISPLAY` (e.g., build lookup maps at module load, keep Tailwind class mappings in the config). Alternatively, export helpers like `getStrategyLetter(id)` from strategyConfig and have Badge.tsx call them. | safe-during-trading |
| M2 | [features/arena/ArenaCard.tsx:127-136](argus/ui/src/features/arena/ArenaCard.tsx#L127) vs [features/trades/TradeTable.tsx](argus/ui/src/features/trades/TradeTable.tsx) | **RESOLVED FIX-12-frontend** — ArenaCard renders the strategy badge inline via `getStrategyDisplay(strategy_id)` with an explicit hex color style; TradeTable/Trade Log renders it via the `<StrategyBadge>` component from `Badge.tsx` (Tailwind class-based). Both derive from different sources (M1), so the same strategy can look different across pages. | Visual inconsistency across pages; future color changes only land in one surface. | After M1 consolidation, route all badge rendering through one component (`<StrategyBadge>`) and have ArenaCard use it. | safe-during-trading |
| M3 | `hooks/*.ts`, `features/*/hooks/*.ts` (~85 `useQuery` call sites) | **RESOLVED FIX-12-frontend** — Query-key shape is inconsistent. Examples: `['account']` ([hooks/useAccount.ts:15](argus/ui/src/hooks/useAccount.ts#L15)), `['catalysts', 'symbol', symbol]` ([hooks/useCatalysts.ts:153](argus/ui/src/hooks/useCatalysts.ts#L153)), `['observatory', 'closest-misses', tierKey, date]` ([features/observatory/hooks/useMatrixData.ts:61](argus/ui/src/features/observatory/hooks/useMatrixData.ts#L61)). Some have a domain prefix, some don't. No enforced convention. | Makes targeted cache invalidation error-prone — `queryClient.invalidateQueries({ queryKey: ['briefings'] })` will miss anything keyed differently. Not actively broken, but a regression surface as pages grow. | Document a keying convention in a query-keys module (e.g., `const qk = { account: () => ['account'] as const, catalystsBySymbol: (s: string) => ['catalysts', 'symbol', s] as const }`), migrate opportunistically during page touches. | safe-during-trading |
| M4 | [hooks/useBriefings.ts:27](argus/ui/src/hooks/useBriefings.ts#L27) | **RESOLVED FIX-12-frontend** — `useBriefings()` polls `/debrief/briefings` every 30s regardless of market hours. Other live-data hooks (`useCatalystsBySymbol`, `useRecentCatalysts`) use `isMarketHours() ? X : false`. This is user-authored journal content that rarely changes — 30s polling outside trading hours is pure overhead. | Wasted requests, JWT-authenticated, hitting the API 2880× per day off-hours for no user benefit. Not a correctness issue. | Gate on `isMarketHours()` or raise to a much longer interval; briefing list realistically only changes when the user edits it (no push needed). | safe-during-trading |
| M5 | `hooks/*` — sampled 5 query hooks across pages | **RESOLVED FIX-12-frontend** — Error UI is ad-hoc. `TradesPage.tsx` renders inline red text; `AIInsightCard.tsx`/`UniverseStatusCard.tsx` render custom Error-component subtrees; `DailyPnlCard.tsx` silently returns `null` on error; Observatory views generally render null. No shared error primitive or error boundary at the page level. | Inconsistent UX when something fails — some cards show a useful message, others just vanish. Hides real backend issues during a session. | Add a shared `<QueryErrorFallback error={e} onRetry={refetch}>` primitive; wrap each page's card grid in an `<ErrorBoundary>`; adopt across sampled sites opportunistically. | safe-during-trading |
| M6 | Same sample as M5 | **RESOLVED FIX-12-frontend** — Loading states mix Skeleton components (TradesPage, BriefingEditor), spinner overlays (UniverseStatusCard), and silent null (Debrief sub-sections, Observatory views). No shared skeleton system. | Project-knowledge lesson from Sprint 17.5: "no conditional skeleton/content swaps — always render same DOM structure." Current mix violates this in places (TradesPage returns a different tree while loading vs loaded). | Pick one pattern per component type (cards → skeleton; tables → skeleton rows; charts → ghost axes). Codify in a `<CardSkeleton>` primitive. | safe-during-trading |
| M7 | [pages/ArenaPage.tsx:99](argus/ui/src/pages/ArenaPage.tsx#L99) | **RESOLVED FIX-12-frontend** — Priority-score threshold for 2-column card spanning is hardcoded: `score > 0.7 ? 2 : 1`. No config or store lookup. The recompute interval (`2000`) on line 106 is also a magic number. | If Arena attention tuning moves to config, UI won't pick it up. Low blast radius today (Arena only). | Hoist to a `constants/arena.ts` or pull from a future `arena.yaml`. | safe-during-trading |
| M8 | [utils/platform.ts:10-96](argus/ui/src/utils/platform.ts#L10) | **RESOLVED FIX-12-frontend** — `isTauri()`/`isPWA()`/platform-detection helpers exist but are not imported anywhere in `argus/ui/src/`. No `@tauri-apps/*` imports, no `invoke()`/`listen()` calls. `CLAUDE.md` still lists "Tauri desktop + PWA mobile" as shipped. | Dead or speculative infrastructure. Either Tauri was deferred and should be documented as such, or the helpers are being held for later integration and should be flagged in the deferred-items table. Not broken — just misaligned with stated architecture. | Either delete `platform.ts` (and update CLAUDE.md to say "PWA; Tauri deferred") or open a DEF tracking Tauri wire-up. | deferred-to-defs |
| M9 | [features/dashboard/VitalsStrip.test.tsx:59](argus/ui/src/features/dashboard/VitalsStrip.test.tsx#L59), [features/orchestrator/StrategyDecisionStream.test.tsx:30](argus/ui/src/features/orchestrator/StrategyDecisionStream.test.tsx#L30), [hooks/__tests__/useQuality.test.tsx:48](argus/ui/src/hooks/__tests__/useQuality.test.tsx#L48), [features/arena/useArenaWebSocket.test.ts:72](argus/ui/src/features/arena/useArenaWebSocket.test.ts#L72) | **RESOLVED FIX-11-backend-api** (spot fix on these 4 files; DEF-167 tracks broader scan as companion to DEF-163) — Hardcoded absolute dates in test fixtures: `'2026-04-02T09:30:00Z'`, `'2026-03-16T10:30:00-04:00'`, `'2026-03-14T10:00:00Z'`, `'2024-01-15T09:30:00Z'`. Same root-cause class as pytest DEF-137/DEF-163 (date decay). `TradeStatsBar.test.tsx:20` uses `new Date().toISOString()` — the correct pattern. | Tests may start failing as real time advances past the fixture dates, especially tests that render relative-time strings ("3 days ago" etc.). | Replace with `new Date()` or `new Date(Date.now() - N*DAY)` for relative-date assertions. Add Vitest equivalent of DEF-163 to the deferred list. | deferred-to-defs |

## LOW Findings

| # | File:Line | Finding | Impact | Suggested Fix | Safety |
|---|-----------|---------|--------|---------------|--------|
| L1 | [features/dashboard/OrchestratorStatusStrip.tsx:26-28](argus/ui/src/features/dashboard/OrchestratorStatusStrip.tsx#L26) | **RESOLVED FIX-12-frontend** — Local `formatPercent()` helper defined inline; duplicates `utils/format.ts` `formatPercent`. Divergence risk if global helper changes. | Cosmetic; currently both do the same thing. | Import from `utils/format` and delete the local copy. | safe-during-trading |
| L2 | `argus/ui/eslint.config.js` | **RESOLVED FIX-12-frontend** — ESLint flat config uses `tseslint.configs.recommended` but does not explicitly enable `no-unused-vars` / `@typescript-eslint/no-unused-vars` as `error`. Unused imports are not caught at lint time. | No unused-import regressions detected in this audit's sample, but the guardrail isn't present. | Add `'@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }]` to the config. | safe-during-trading |
| L3 | [vitest.config.ts:7-15](argus/ui/vitest.config.ts#L7) | **RESOLVED FIX-12-frontend** — `pool` / `poolOptions` not set — Vitest default is `forks`, but making it explicit prevents silent regressions on Vitest upgrades. | None today. | Add `pool: 'forks'` explicitly. Optional: add a short comment pointing at `pkill -f "vitest/dist/workers"` for when workers orphan. | safe-during-trading |
| L4 | `hooks/*.ts` — polling cadence | **RESOLVED FIX-12-frontend** — `useAccount` refetches every 5s ([hooks/useAccount.ts:18](argus/ui/src/hooks/useAccount.ts#L18)), `usePositions` every 15s ([hooks/usePositions.ts:19](argus/ui/src/hooks/usePositions.ts#L19)), Observatory resources range 5s–60s with no coordination ([features/observatory/hooks/useSymbolDetail.ts](argus/ui/src/features/observatory/hooks/useSymbolDetail.ts)). All are authenticated JWT requests. | Steady-state API chatter. Not a correctness issue. | Document expected cadence per data type in a central constants file; align where there's no reason for variance. | safe-during-trading |
| L5 | [pages/PerformancePage.tsx:23](argus/ui/src/pages/PerformancePage.tsx#L23) | **RESOLVED-VERIFIED FIX-12-frontend** — `Component` imported from React used only as a class base (line ~69–93 class ChartErrorBoundary), never referenced directly in JSX of the file. Functionally correct but lint-adjacent. | None. | Non-issue once L2 is enabled with appropriate config — ESLint understands class inheritance usage. | safe-during-trading |

## COSMETIC Findings

| # | File:Line | Finding | Impact | Suggested Fix | Safety |
|---|-----------|---------|--------|---------------|--------|
| K1 | [components/Badge.tsx:110](argus/ui/src/components/Badge.tsx#L110) | Inline comment `// Afternoon Momentum → A` is orphaned — the line above defines `momentum: 'A'`, both entries below say the same thing. | Noise. | Drop the comment or move it to the `afternoon_momentum:` line only. | safe-during-trading |
| K2 | [components/Badge.tsx:1-10](argus/ui/src/components/Badge.tsx#L1) | Docblock says "17-D from UX Feature Backlog" and lists only 4 strategies ("ORB=blue, Scalp=purple, VWAP=teal, Momentum=amber"). Reality: 12 strategies with distinct colors. Stale by ~7 strategy additions. | Misleading onboarding for anyone reading the top of the file. | Update or delete the summary. | safe-during-trading |
| K3 | [features/arena/useArenaWebSocket.ts](argus/ui/src/features/arena/useArenaWebSocket.ts) and three Observatory WS hooks | None of the WS hooks reconnect on disconnect. Arena shows the overlay ("Connection lost — reconnecting…") but the hook does not actually reconnect — the text is aspirational. User must refresh. | Minor UX mismatch between copy and behavior. The singleton `WebSocketClient` in `api/ws.ts` *does* have exponential backoff, but it is unused. | Either implement reconnect (preferred — use `api/ws.ts` pattern) or change the overlay copy to "Connection lost — reload to retry". | safe-during-trading |

---

## Positive Observations

Per-page highlights of what is working well and should be preserved:

- **Dashboard — VitalsStrip formatting:** Win-rate handling is correct end-to-end. [VitalsStrip.tsx](argus/ui/src/features/dashboard/VitalsStrip.tsx), [TodayStats.tsx:126](argus/ui/src/features/dashboard/TodayStats.tsx#L126), [TradeStatsBar.tsx:52](argus/ui/src/features/trades/TradeStatsBar.tsx#L52), [MetricsGrid.tsx:34](argus/ui/src/features/performance/MetricsGrid.tsx#L34), [StrategyBreakdown.tsx:49](argus/ui/src/features/performance/StrategyBreakdown.tsx#L49), [PatternCard.tsx:105](argus/ui/src/features/patterns/PatternCard.tsx#L105), [ExperimentsPage.tsx:200](argus/ui/src/pages/ExperimentsPage.tsx#L200) — every rendered `win_rate` either multiplies by 100 before `formatPercentRaw()` or uses `formatPercent()` (which multiplies internally). DEF-116 is cleanly resolved across 10+ call sites. No secondary 0–1 proportion fields (`accuracy_score`, `confidence_score`, etc.) were found on the frontend API surface.
- **Trade Log — tab shortcuts:** [TradesPage.tsx:49-58](argus/ui/src/pages/TradesPage.tsx#L49) — `l`/`s` tab switch respects input focus, matches keyboard-shortcut conventions used across the app.
- **Performance — chart error isolation:** [PerformancePage.tsx:69-93](argus/ui/src/pages/PerformancePage.tsx#L69) — ChartErrorBoundary wraps Recharts so a bad data point doesn't take down the page. Good pattern to replicate on Observatory views (see M5).
- **The Arena — WebSocket cleanup and batching:** [useArenaWebSocket.ts:333-341](argus/ui/src/features/arena/useArenaWebSocket.ts#L333) cancels pending `rAF`, closes the socket, clears refs. rAF batching at [:196-200](argus/ui/src/features/arena/useArenaWebSocket.ts#L196) preserved for 60fps. [MiniChart.tsx:148](argus/ui/src/features/arena/MiniChart.tsx#L148) uses `createSeriesMarkers()` — v5 plugin pattern (not deprecated v4 `setMarkers()`). Explicit disconnection overlay at [ArenaPage.tsx:161-170](argus/ui/src/pages/ArenaPage.tsx#L161) (not silent failure — see K3 caveat on reconnect).
- **Orchestrator — StrategyDecisionStream:** Hook mocked at test level; telemetry slide-out panel pattern is clean and reusable.
- **Pattern Library — PatternCardGrid:** [PatternCardGrid.tsx:52-75](argus/ui/src/features/patterns/PatternCardGrid.tsx#L52) — `useMemo` chains for filter→sort are tight and both results are read in render (no dead memos).
- **The Debrief — CRUD hooks:** [hooks/useBriefings.ts:45-84](argus/ui/src/hooks/useBriefings.ts#L45) — clean mutation + `queryClient.invalidateQueries` pattern with targeted key invalidation. (The only issue here is the polling cadence, M4.)
- **System — usePipelineStatus (DEC-329):** [hooks/usePipelineStatus.ts:23-32](argus/ui/src/hooks/usePipelineStatus.ts#L23) is correctly propagated to every catalyst/intelligence-briefing query: [useCatalysts.ts:155,174](argus/ui/src/hooks/useCatalysts.ts#L155), [useIntelligenceBriefings.ts:120,138](argus/ui/src/hooks/useIntelligenceBriefings.ts#L120). No 503-spam regression. Also: market-hours gating on catalyst polling is a good pattern that should extend to M4.
- **Observatory — Three.js disposal chain:** [FunnelSymbolManager.ts:339-353](argus/ui/src/features/observatory/views/three/FunnelSymbolManager.ts#L339) + [FunnelScene dispose:258-292] disposes geometry, material, InstancedMesh, WebGLRenderer, CSS2DRenderer, cancels animation frames, removes DOM nodes. Shared-scene pattern between Funnel and Radar is single-renderer, camera-transition only. Lazy-loaded at [ObservatoryPage.tsx:24-31](argus/ui/src/features/observatory/ObservatoryPage.tsx#L24) via `React.lazy` so Three.js stays out of the main bundle. Text-book Three.js lifecycle management.
- **Experiments — lazy-loaded:** Kept out of main bundle via `React.lazy` in [App.tsx](argus/ui/src/App.tsx).
- **App-wide WebSocket test coverage:** All three consumers of `useArenaWebSocket` — [ArenaPage.test.tsx:19](argus/ui/src/pages/ArenaPage.test.tsx#L19), [arenaAnimations.test.tsx:82](argus/ui/src/features/arena/arenaAnimations.test.tsx#L82), and the hook's own test — `vi.mock` the hook at module level. DEF-138 regression class is blocked for Arena. (Observatory WS hooks lack dedicated tests, but their consumers don't render in the test tree without intent.)
- **Vitest hygiene:** `testTimeout: 10_000` + `hookTimeout: 10_000` at [vitest.config.ts:13-14](argus/ui/vitest.config.ts#L13) are adequate for the hook lifecycle; `src/test/setup.ts` mocks `framer-motion`, `ResizeObserver`, `matchMedia`. `pkill -f "vitest/dist/workers"` documented in Sprint 32.8 session notes and `workflow/templates/implementation-prompt.md`.
- **PWA manifest + service worker:** [public/manifest.json](argus/ui/public/manifest.json) is complete (192/512 icons, maskable, theme color). [public/sw.js](argus/ui/public/sw.js) correctly uses network-only for `/api/` and `/ws/` — trading data is never cached. `v3` cache versioning. Registered only in production ([main.tsx:31-37](argus/ui/src/main.tsx#L31)).
- **Router coverage:** All 10 pages reachable via sidebar and keyboard 1–9+0; Trade Log sub-tabs via `l`/`s`; Observatory sub-views via `f`/`m`/`r`/`t`/`[`/`]`/`Tab`/`/`. No orphaned routes or unmapped shortcuts.
- **Strategy config defensive fallback:** [strategyConfig.ts:170-194](argus/ui/src/utils/strategyConfig.ts#L170) — `getStrategyDisplay()` generates a best-effort title-cased name + first-letter + grey color for unknown strategy IDs. This is why C1 is visible but not fatal — the new strategies show up as ugly grey badges rather than crashing the app. Worth keeping as belt-and-suspenders even after C1 is fixed.

---

## Statistics

- **Files deep-read:** ~35 (strategyConfig.ts, Badge.tsx, App.tsx, Sidebar.tsx, AppShell.tsx, ArenaPage.tsx, useArenaWebSocket.ts, MiniChart.tsx, FunnelView.tsx, FunnelScene.ts, FunnelSymbolManager.ts, useMatrixData.ts, useSessionVitals.ts, useBriefings.ts, useCatalysts.ts, useIntelligenceBriefings.ts, usePipelineStatus.ts, useAccount.ts, usePositions.ts, useQuality.test.tsx, VitalsStrip.tsx, TodayStats.tsx, TradesPage.tsx, TradeStatsBar.tsx, TradeTable.tsx, ArenaCard.tsx, MetricsGrid.tsx, StrategyBreakdown.tsx, ObservatoryPage.tsx, utils/platform.ts, utils/format.ts, OrchestratorStatusStrip.tsx, vitest.config.ts, manifest.json, sw.js, api/ws.ts, api/client.ts excerpt)
- **Files skimmed:** ~80 (sample across 10 pages + shared UI primitives + test files + feature hooks)
- **Total findings:** 18 (1 critical, 9 medium, 5 low, 3 cosmetic)
- **Safety distribution:** 14 safe-during-trading / 0 weekend-only / 2 deferred-to-defs / 2 read-only-no-fix-needed (C1 and M1 are frontend-only, no backend runtime impact; M8 + M9 go to DEF table)
- **Estimated Phase 3 fix effort:** 2–3 sessions
  - **Session A** (strategy identity): C1 + M1 + M2 bundled — consolidate to single source of truth in `strategyConfig.ts`, register 3 Sprint 31A strategies, route Badge.tsx through strategyConfig, refactor ArenaCard to use `<StrategyBadge>`. ~1 session, safe-during-trading.
  - **Session B** (query/loading/error pattern hygiene): M3 + M4 + M5 + M6 + L4 — introduce `constants/queryKeys.ts`, `<QueryErrorFallback>`, `<CardSkeleton>`, migrate opportunistically. Can be split into 2 smaller sessions if needed.
  - **Session C** (misc cleanup): M7, L1, L2, L3, L5, K1, K2, K3 — tiny diffs, bundle into one session.
  - M8 + M9 → deferred to DEF table, no Phase 3 session needed.
