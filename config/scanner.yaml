# Scanner Configuration
# Controls pre-market stock selection

# Scanner type: "static" for replay/backtest, "alpaca" for live trading
scanner_type: "static"

# Static scanner symbols (used when scanner_type = "static")
static_symbols:
  - "AAPL"
  - "MSFT"
  - "NVDA"
  - "TSLA"
  - "AMD"

# Alpaca scanner configuration (used when scanner_type = "alpaca")
alpaca_scanner:
  universe_source: "config"  # Use universe_symbols list
  universe_symbols:
    - "AAPL"
    - "MSFT"
    - "NVDA"
    - "TSLA"
    - "AMD"
    - "AMZN"
    - "META"
    - "GOOGL"
    - "NFLX"
    - "SPY"
  min_price: 5.0
  max_price: 500.0
  min_volume_yesterday: 1000000
  max_symbols_returned: 10

# Databento scanner configuration (used when data_source = "databento")
databento_scanner:
  universe_symbols:
    - "AAPL"
    - "MSFT"
    - "NVDA"
    - "TSLA"
    - "AMD"
    - "AMZN"
    - "META"
    - "GOOGL"
    - "NFLX"
    - "SPY"
  min_gap_pct: 0.02
  min_price: 10.0
  max_price: 500.0
  min_volume: 1000000
  max_symbols_returned: 10
  dataset: "XNAS.ITCH"
