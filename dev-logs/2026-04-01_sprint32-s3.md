# Sprint 32, Session 3: Runtime Wiring

**Date:** 2026-04-01
**Type:** Implementation
**Sprint:** 32 — Parameterized Templates + Experiment Pipeline

## Summary

Replaced 7 hardcoded PatternModule constructors in main.py with factory calls,
extended PatternBacktester to support all 7 patterns via factory (DEF-121
resolved), added config_fingerprint column to the trades table, and wired
fingerprint storage through Trade model and TradeLogger.

## Changes

- `argus/main.py`: removed direct pattern class imports; 7 constructors now use
  `build_pattern_from_config()`; fingerprint set on each strategy post-construction
- `argus/backtest/vectorbt_pattern.py`: added `_load_pattern_config()` helper;
  `_create_pattern_by_name()` now delegates to factory (all 7 patterns supported)
- `argus/strategies/pattern_strategy.py`: `_config_fingerprint` attribute +
  `config_fingerprint` property (minimal change, set externally by main.py)
- `argus/models/trading.py`: `config_fingerprint: str | None = None` on Trade
- `argus/db/schema.sql`: `config_fingerprint TEXT` nullable column in trades DDL
- `argus/db/manager.py`: idempotent ALTER TABLE migration for config_fingerprint
- `argus/analytics/trade_logger.py`: config_fingerprint in INSERT + _row_to_trade

## Tests

- `tests/test_runtime_wiring.py`: 33 new tests (9 logical cases, parametrized)
- Full suite: 4321 passed, 0 failures

## Deferred Items Resolved

- DEF-121: PatternBacktester `_create_pattern_by_name()` now supports all 7
  patterns (was 3: bull_flag, flat_top_breakout, abcd)

## Reviewer Verdict

CLEAR — 3 low-severity findings, none requiring fixes.
