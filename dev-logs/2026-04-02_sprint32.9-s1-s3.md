# Sprint 32.9 Sessions 1–3

**Date:** 2026-04-02  
**Sprint:** 32.9 — Position Safety + Quality Recalibration + Strategy Triage

## What Changed

### Session 1 — EOD Flatten Timeout + Position Safety
- `OrderManager`: configurable `eod_flatten_timeout_seconds` (default 30s), retry loop with `max_flatten_retries`
- `OrderManager`: margin rejection circuit breaker — tracks consecutive margin rejections, auto-flattens open positions when threshold hit
- New config keys in `config/order_manager.yaml`: `eod_flatten_timeout_seconds`, `margin_rejection_threshold`, `margin_circuit_reset_positions`
- New tests in `tests/execution/test_order_manager_sprint329.py`

### Session 3 — Quality Recalibration + Config Triage
- **Pre-EOD signal cutoff:** `signal_cutoff_enabled`/`signal_cutoff_time` on `OrchestratorConfig`; cutoff check at top of `_process_signal()` gates new entries after 15:30 ET
- **Quality engine recalibration:** `historical_match` weight zeroed (was constant 50.0 stub); weight redistributed to `pattern_strength` (0.375) and `volume_profile` (0.275); thresholds recalibrated for actual ~35-77 score range
- **Max concurrent positions:** `max_concurrent_positions: 50` (was 0/disabled)
- **Overflow capacity:** `broker_capacity: 50` (was 60)
- **Strategy demotion:** ABCD + Flat-Top Breakout → `mode: shadow` (21.9% and 18.8% win rates)
- **Experiment pipeline:** `enabled: true` (no-op with empty `variants: {}`)
- **Pre-live checklist:** Sprint 32.9 section added (12 items)
- New tests: 15 new (`tests/core/test_signal_cutoff.py` + `tests/intelligence/test_quality_config.py` additions)

## Test Counts
- pytest: 4539 → 4567 passing (+28)
- Vitest: unchanged (846)
- 1 pre-existing S1-2 regression in `test_order_manager_reconstruction_with_positions` (shares_remaining=1 vs expected 100)

## Decisions
- Signal cutoff uses `getattr(self, "_clock", None)` guard to remain compatible with integration tests that construct `ArgusSystem` via `__new__`
- Historical match weight zeroed rather than redistributed evenly — concentrated the benefit in the two dimensions that actually vary with signal quality
